"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.STATS_TYPES = exports.FILTER_NAMES = exports.COMPLETION_TYPES = void 0;
exports.addOptionToFilter = addOptionToFilter;
exports.getSelectedOptionsText = getSelectedOptionsText;
exports.removeOptionFromFilter = removeOptionFromFilter;
function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }
function _iterableToArray(iter) { if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter); }
function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) return _arrayLikeToArray(arr); }
function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i]; return arr2; }
var COMPLETION_TYPES = Object.freeze({
  ITEM: 'ITEM',
  LIST: 'LIST'
});
exports.COMPLETION_TYPES = COMPLETION_TYPES;
var FILTER_NAMES = Object.freeze({
  all: 'all',
  assigneeIds: 'assigneeIds',
  brandLocationGroups: 'brandLocationGroups',
  coops: 'coops',
  customFilter1: 'customFilter1',
  customFilter2: 'customFilter2',
  dateFilterOption: 'dateFilterOption',
  dateFilterRange: 'dateFilterRange',
  groupBy: 'groupBy',
  hasActiveListInstances: 'hasActiveListInstances',
  itemFlagIds: 'itemFlagIds',
  listTemplateIds: 'listTemplateIds',
  locationGroupIds: 'locationGroupIds',
  locationIds: 'locationIds',
  referencedByIds: 'referencedByIds',
  saved: 'saved',
  sortOrder: 'sortOrder',
  statsType: 'statsType'
});
exports.FILTER_NAMES = FILTER_NAMES;
var STATS_TYPES = Object.freeze({
  CLOSED: 'CLOSED',
  OPEN: 'OPEN'
});

/**
 * Internal filter state management to add a selected option
 *
 * @param {string} filterName - name of the filter
 * @param {function} filterSetter - state setter function
 * @param {object} filterOptions - current options of filter
 * @param {object} option - new option to add
 * @param {bool} limitToOneOption - if the option array should be limited to 1
 */
exports.STATS_TYPES = STATS_TYPES;
function addOptionToFilter(filterName, filterSetter, filterOptions, option, limitToOneOption) {
  var value = [];
  if (Array.isArray(filterOptions) && !(filterName === FILTER_NAMES.locationIds && limitToOneOption) && !(filterName === FILTER_NAMES.listTemplateIds && limitToOneOption)) {
    value = _toConsumableArray(filterOptions);
  }
  value.push(option);
  filterSetter(value);
}

/**
 * Generate button text based on selected options
 *
 * @param {array} selectedItems - An array of the selected options.
 * @param {string} selectedType - A string of the selection type, like 'Location' or 'List'
 * @param {boolean} isSingleSelection - If set to true the filter only allows for a single item to be selected
 * @param {number} count - total possible options
 *
 * @return {string} text
 */
function getSelectedOptionsText(selectedItems, selectedType) {
  var isSingleSelection = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
  var count = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : null;
  if (selectedItems.length === 1) {
    if (isSingleSelection) {
      return selectedItems[0].label;
    }
    return "".concat(selectedItems.length, " ").concat(selectedType);
  }
  if (selectedItems.length > 1) {
    return "".concat(selectedItems.length, " ").concat(selectedType, "s");
  }
  if (isSingleSelection) {
    return "Select ".concat(selectedType);
  }
  if (count === null) {
    return "All ".concat(selectedType, "s");
  }
  return "All ".concat(selectedType, "s (").concat(count, ")");
}

/**
 * Internal filter state management to remove a selected option
 *
 * @param {string} filterName - name of the filter
 * @param {function} filterSetter - state setter function
 * @param {object} filterOptions - current options of filter
 * @param {object} id - id of option to remove
 */
function removeOptionFromFilter(filterName, filterSetter, filterOptions, id) {
  var value = _toConsumableArray(filterOptions);
  var index = value.findIndex(function (anOption) {
    return anOption.id === id;
  });
  value = [].concat(_toConsumableArray(filterOptions.slice(0, index)), _toConsumableArray(filterOptions.slice(index + 1)));
  filterSetter(value);
}