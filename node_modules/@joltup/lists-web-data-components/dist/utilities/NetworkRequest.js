"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.apolloClient = void 0;
exports.getGraphQLApiUrl = getGraphQLApiUrl;
exports.graphQLApiPath = void 0;
var _client = require("@apollo/client");
var _cache = require("@apollo/client/cache");
var _error = require("@apollo/client/link/error");
var _listsCore = require("@joltup/lists-core");
var _crossFetch = _interopRequireDefault(require("cross-fetch"));
var _2 = require(".");
function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
var graphQLApiPath = '/graphql';

/**
 * Returns the appropriate url to the graphQL api.
 *
 * @return {string} url - The appropriate url to the graphQL api.
 */
exports.graphQLApiPath = graphQLApiPath;
function getGraphQLApiUrl() {
  return "".concat(localStorage.getItem(_2.LS_KEYS.API_HOST)).concat(graphQLApiPath);
}
var cache = new _cache.InMemoryCache({
  /**
   * Used to create custom IDs for objects in the cache
   * https://www.apollographql.com/docs/react/caching/cache-configuration/#data-normalization
   *
   * Objects in the cache are uniquely identified based on only ID and __typename
   * On the comparison report this is an issue because the same ID
   *  can have different listInstanceStats based on filters.
   *
   * @param {object} object - data object
   *
   * @returns {string} id - data id from object
   */
  dataIdFromObject: function dataIdFromObject(object) {
    var __typename = object.__typename;
    var listInstanceStatsObjects = ['Location', 'ListTemplate'];
    if (listInstanceStatsObjects.includes(__typename) && object.listInstanceStats) {
      return "".concat((0, _cache.defaultDataIdFromObject)(object), ":").concat(JSON.stringify(object.listInstanceStats));
    }
    return (0, _cache.defaultDataIdFromObject)(object);
  }
});

// This code defines an instance of the `HttpLink` class from the `apollo-link-http`
// package. This link will be used to connect to a GraphQL API over HTTP.
var httpLink = new _client.HttpLink({
  credentials: 'include',
  fetch: _crossFetch.default,
  headers: {
    jolt_companyid: window.companyId || localStorage.getItem(_2.LS_KEYS.COMPANY_ID)
  },
  uri: getGraphQLApiUrl()
});

// This code defines an Apollo Link that will be used to handle errors that occur
// during GraphQL operations. The `onError` function takes a callback that will be
// called whenever an error occurs. The callback receives an object that contains
// information about any GraphQL or network errors that occurred.
var onErrorLink = (0, _error.onError)(function (_ref) {
  var graphQLErrors = _ref.graphQLErrors,
    networkError = _ref.networkError;
  if (graphQLErrors) {
    console.error("[Jolt GraphQL Error]: ".concat(JSON.stringify(graphQLErrors))); // eslint-disable-line no-console

    if (graphQLErrors.includes('["token invalid"]') || graphQLErrors.includes('["jwt expired"]')) {
      (0, _listsCore.refreshAuthToken)();
    }
  }
  if (networkError) {
    console.error("[Network error]: ".concat(networkError)); // eslint-disable-line no-console
  }
});

var apolloClient = new _client.ApolloClient({
  cache: cache,
  cacheRedirects: {
    Query: {
      itemTemplate: function itemTemplate(_, args, _ref2) {
        var getCacheKey = _ref2.getCacheKey;
        return getCacheKey({
          __typename: 'ItemTemplate',
          id: args.id
        });
      }
    }
  },
  link: (0, _client.from)([onErrorLink, httpLink]),
  resolvers: {}
});
exports.apolloClient = apolloClient;