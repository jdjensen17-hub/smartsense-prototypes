"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.settingsFilter = void 0;
var _mode = require("./mode");
var modesMap = Object.freeze({
  Company: 'COMPANY',
  ContentGroup: 'CONTENT_GROUP',
  Location: 'LOCATION'
});
var windowModeInput = window.modeInput || {
  id: null
};
var windowMode = modesMap[windowModeInput.mode];
var modeInput = (0, _mode.getModeInput)();

/**
 * Returns the filter variable for the settings graphQL query
 *
 * @returns {object}
 */
var settingsFilter = {
  // ignored because I don't know when COMPANY is used as mode but keeping it for backwards compatibility
  mode: windowMode === modesMap.Company ? /* istanbul ignore next */windowMode : modeInput.mode,
  id: modeInput.id
};
exports.settingsFilter = settingsFilter;