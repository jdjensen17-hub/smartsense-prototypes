"use strict";

function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.OPTION_TYPES = void 0;
exports.convertLocationsToFilterOptions = convertLocationsToFilterOptions;
exports.getLocationOptions = getLocationOptions;
exports.handleOnAdd = handleOnAdd;
exports.handleOnClear = handleOnClear;
exports.handleOnRemove = handleOnRemove;
exports.setInitialSelectedLocation = setInitialSelectedLocation;
var _lodash = require("lodash");
var _utilities = require("../../utilities");
function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); enumerableOnly && (symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; })), keys.push.apply(keys, symbols); } return keys; }
function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = null != arguments[i] ? arguments[i] : {}; i % 2 ? ownKeys(Object(source), !0).forEach(function (key) { _defineProperty(target, key, source[key]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } return target; }
function _defineProperty(obj, key, value) { key = _toPropertyKey(key); if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }
function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return _typeof(key) === "symbol" ? key : String(key); }
function _toPrimitive(input, hint) { if (_typeof(input) !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (_typeof(res) !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _unsupportedIterableToArray(arr, i) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArrayLimit(arr, i) { var _i = null == arr ? null : "undefined" != typeof Symbol && arr[Symbol.iterator] || arr["@@iterator"]; if (null != _i) { var _s, _e, _x, _r, _arr = [], _n = !0, _d = !1; try { if (_x = (_i = _i.call(arr)).next, 0 === i) { if (Object(_i) !== _i) return; _n = !1; } else for (; !(_n = (_s = _x.call(_i)).done) && (_arr.push(_s.value), _arr.length !== i); _n = !0); } catch (err) { _d = !0, _e = err; } finally { try { if (!_n && null != _i.return && (_r = _i.return(), Object(_r) !== _r)) return; } finally { if (_d) throw _e; } } return _arr; } }
function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }
function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }
function _iterableToArray(iter) { if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter); }
function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) return _arrayLikeToArray(arr); }
function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i]; return arr2; }
var OPTION_TYPES = {
  HEADER: 'header',
  OPTION: 'option'
};

/**
 * Convert locations to filter options.
 *
 * @param {array} locations - An array of all the available locations.
 *
 * @returns {array} options - A list options for a dropdown.
 */
exports.OPTION_TYPES = OPTION_TYPES;
function convertLocationsToFilterOptions(locations) {
  if ((0, _lodash.isNil)(locations)) {
    return [];
  }
  return locations.map(function (_ref) {
    var id = _ref.id,
      name = _ref.name,
      optionType = _ref.optionType;
    return {
      id: id,
      label: name,
      optionType: optionType
    };
  });
}

/**
 * Returns the location filter options, with grouping headers if both locationGroups and locations are set
 *
 * @param {Object[]} locationGroups The locationGroup options
 * @param {Object[]} locations The location options
 *
 * @returns {Object[]} The options to use for the filter
 */
function getLocationOptions() {
  var locationGroups = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
  var locations = arguments.length > 1 ? arguments[1] : undefined;
  // If there are no location groups, just return the locations
  if (locationGroups.length === 0) {
    return locations;
  }
  var locationGroupsHeader = Object.freeze({
    id: 'locationGroups',
    name: 'Organizations',
    optionType: OPTION_TYPES.HEADER
  });
  var locationsHeader = Object.freeze({
    id: 'locations',
    name: 'Locations',
    optionType: OPTION_TYPES.HEADER
  });

  // Return the options with headers
  return [locationGroupsHeader].concat(_toConsumableArray(locationGroups), [locationsHeader], _toConsumableArray(locations));
}

/**
 * Prepend filter name to callback parameters onAdd.
 *
 * @param {function} callback - Callback function.
 * @param {function} logger - Logging function.
 * @param {function} onClose - The function to close the filter
 * @param {boolean} isSingleSelection - If the filter only allows
 * @param {function} setter - Internal filter state options setter
 * @param {array} options - Current filter state optionsselecting a single value
 *
 * @returns {function} callback - Callback function.
 */
function handleOnAdd(callback) {
  var logger = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : function () {
    return null;
  };
  var onClose = arguments.length > 2 ? arguments[2] : undefined;
  var isSingleSelection = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : false;
  var setter = arguments.length > 4 ? arguments[4] : undefined;
  var options = arguments.length > 5 ? arguments[5] : undefined;
  return function (value) {
    logger();
    var filterName = _utilities.FILTER_NAMES.locationIds;

    // Try to base64 decode the ID to determine if it is a location group
    try {
      var _atob$split = atob(value.id).split(':'),
        _atob$split2 = _slicedToArray(_atob$split, 1),
        idType = _atob$split2[0];
      if (idType === 'LocationGroup') {
        filterName = _utilities.FILTER_NAMES.locationGroupIds;
      }
      // eslint-disable-next-line no-empty
    } catch (_unused) {} // Do nothing if the id is not a base64 encoded string

    if (isSingleSelection) {
      onClose();
    }
    return callback(filterName, setter, options, _objectSpread(_objectSpread({}, value), {}, {
      filterName: filterName
    }), isSingleSelection);
  };
}

/**
 * Handles clearing the selected items.
 *
 * @param {function} callback - Callback function.
 * @param {function} logger - Logging function.
 * @param {function} setter - Internal filter state options setter
 * @param {array} options - Current filter state options
 *
 * @returns {function} callback - Callback function.
 */
function handleOnClear(callback) {
  var logger = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : function () {
    return null;
  };
  var setter = arguments.length > 2 ? arguments[2] : undefined;
  var options = arguments.length > 3 ? arguments[3] : undefined;
  return function (value) {
    logger();
    return callback('', setter, options, value);
  };
}

/**
 * Prepend filter name to callback parameters onRemove.
 *
 * @param {function} callback - Callback function.
 * @param {function} logger - Logging function.
 * @param {function} setter - Internal filter state options setter
 * @param {array} options - Current filter state options
 *
 * @returns {function} callback - Callback function.
 */
function handleOnRemove(callback) {
  var logger = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : function () {
    return null;
  };
  var setter = arguments.length > 2 ? arguments[2] : undefined;
  var options = arguments.length > 3 ? arguments[3] : undefined;
  return function (value) {
    logger();
    var filterName = _utilities.FILTER_NAMES.locationIds;

    // Try to base64 decode the ID to determine if it is a location group
    try {
      var _atob$split3 = atob(value).split(':'),
        _atob$split4 = _slicedToArray(_atob$split3, 1),
        idType = _atob$split4[0];
      if (idType === 'LocationGroup') {
        filterName = _utilities.FILTER_NAMES.locationGroupIds;
      }
      // eslint-disable-next-line no-empty
    } catch (_unused2) {} // Do nothing if the id is not a base64 encoded string

    return callback(filterName, setter, options, value);
  };
}

/**
 * If in location mode we'll set the initial default location to the current one
 * @param {object}.{function} addOptionToFilter - Function to add location to selected
 * @param {object}.{boolean} canInitializeDefaultLocation - If default location can be initialized
 * @param {object}.{object} currentLocation - Data about current location
 * @param {object}.{integer} selectedSize - Number of already selected items
 * @param {object}.{function} setCanInitializeDefaultLocation - Sets canInitializeDefaultLocation
 */
function setInitialSelectedLocation(_ref2) {
  var addOptionToFilter = _ref2.addOptionToFilter,
    canInitializeDefaultLocation = _ref2.canInitializeDefaultLocation,
    currentLocation = _ref2.currentLocation,
    selectedSize = _ref2.selectedSize,
    setCanInitializeDefaultLocation = _ref2.setCanInitializeDefaultLocation;
  if (canInitializeDefaultLocation && selectedSize === 0 && currentLocation && !(0, _lodash.isEmpty)(currentLocation)) {
    var _currentLocation$loca = currentLocation.location,
      id = _currentLocation$loca.id,
      name = _currentLocation$loca.name;
    setCanInitializeDefaultLocation(false);
    addOptionToFilter({
      id: id,
      label: name
    });
  }
}