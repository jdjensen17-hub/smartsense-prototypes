"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.convertBrandLocationGroupsToFilterOptions = convertBrandLocationGroupsToFilterOptions;
exports.prependFilterName = prependFilterName;
var _lodash = require("lodash");
var _utilities = require("../../utilities");
/**
 * Convert Brand Location Groups to filter options.
 *
 * @param {array} brandLocationGroups - An array of all the available Brand Location Groups.
 *
 * @return {array} options - A list options for a dropdown.
 */
function convertBrandLocationGroupsToFilterOptions(brandLocationGroups) {
  if ((0, _lodash.isNil)(brandLocationGroups)) {
    return [];
  }
  return brandLocationGroups.map(function (list) {
    return {
      id: list.name,
      label: list.name
    };
  });
}

/**
 * Prepend filter name to callback parameters.
 *
 * @param {function} callback - Callback function.
 * @param {function} logger - Logging function.
 *
 * @returns {function} callback - Callback function.
 */
function prependFilterName(callback) {
  var logger = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : function () {
    return null;
  };
  return function (value) {
    logger();
    return callback(_utilities.FILTER_NAMES.brandLocationGroups, value);
  };
}