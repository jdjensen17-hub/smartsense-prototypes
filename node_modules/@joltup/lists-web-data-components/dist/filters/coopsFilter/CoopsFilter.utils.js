"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.convertCoopsToFilterOptions = convertCoopsToFilterOptions;
exports.prependFilterName = prependFilterName;
var _lodash = require("lodash");
var _utilities = require("../../utilities");
/**
 * Convert Coops to filter options.
 *
 * @param {array} coops - An array of all the available Coops.
 *
 * @return {array} options - A list options for a dropdown.
 */
function convertCoopsToFilterOptions(coops) {
  if ((0, _lodash.isNil)(coops)) {
    return [];
  }
  return coops.map(function (list) {
    return {
      id: list.name,
      label: list.name
    };
  });
}

/**
 * Prepend filter name to callback parameters.
 *
 * @param {function} callback - Callback function.
 * @param {function} logger - Logging function.
 *
 * @returns {function} callback - Callback function.
 */
function prependFilterName(callback) {
  var logger = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : function () {
    return null;
  };
  return function (value) {
    logger();
    return callback(_utilities.FILTER_NAMES.coops, value);
  };
}