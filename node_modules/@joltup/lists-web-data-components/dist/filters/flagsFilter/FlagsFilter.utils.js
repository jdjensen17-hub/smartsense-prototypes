"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.convertFlagsToFilterOptions = convertFlagsToFilterOptions;
exports.prependFilterName = prependFilterName;
var _lodash = require("lodash");
var _utilities = require("../../utilities");
/**
 * Convert flags to filter options.
 *
 * @param {array} flags - An array of all the available flags.
 *
 * @return {array} options - A list options for a dropdown.
 */
function convertFlagsToFilterOptions(flags) {
  if ((0, _lodash.isNil)(flags)) {
    return [];
  }
  return flags.map(function (flag) {
    return {
      id: flag.id,
      label: flag.name
    };
  });
}

/**
 * Prepend filter name to callback parameters.
 *
 * @param {function} callback - Callback function.
 * @param {function} setter - Internal filter state options setter
 * @param {array} options - Current filter state options
 * @param {function} logger - Logging function.
 *
 * @returns {function} callback - Callback function.
 */
function prependFilterName(callback, setter, options) {
  var logger = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : function () {
    return null;
  };
  return function (value) {
    logger();
    return callback(_utilities.FILTER_NAMES.itemFlagIds, setter, options, value);
  };
}