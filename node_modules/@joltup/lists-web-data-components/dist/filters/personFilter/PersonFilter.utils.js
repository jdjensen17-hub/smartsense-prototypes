"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.convertPeopleToOptions = convertPeopleToOptions;
exports.filterOptionsBySearchString = filterOptionsBySearchString;
exports.getFullName = getFullName;
exports.getOptionsById = void 0;
exports.prependFilterName = prependFilterName;
exports.sortPeopleByName = sortPeopleByName;
/**
 * Combines first and last names to get full name
 *
 * @param {Object} person - person object
 *
 * @returns {String} fullName
 */
function getFullName() {
  var _ref = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {},
    _ref$firstName = _ref.firstName,
    firstName = _ref$firstName === void 0 ? '' : _ref$firstName,
    _ref$lastName = _ref.lastName,
    lastName = _ref$lastName === void 0 ? '' : _ref$lastName;
  var needSpace = firstName.length && lastName.length;
  return "".concat(firstName).concat(needSpace ? ' ' : '').concat(lastName);
}

/**
 * Converts graphql people to options for the filter
 *
 * @param {Array} people - people array from graphql query
 *
 * @returns {Array} options
 */
function convertPeopleToOptions() {
  var people = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
  return people.map(function (person) {
    var id = person.id;
    return {
      id: id,
      label: getFullName(person)
    };
  });
}

/**
 * Filters option names by search string
 *
 * @param {Array} options - current filter options
 * @param {String} searchString - search string
 *
 * @returns {Array} filtered
 */
function filterOptionsBySearchString() {
  var options = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
  var searchString = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : '';
  if (searchString.length > 0 && Array.isArray(options)) {
    return options.filter(function (_ref2) {
      var label = _ref2.label;
      return label.toLocaleLowerCase().includes(searchString);
    });
  }
  return options;
}
var getOptionsById = function getOptionsById() {
  var options = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
  var ids = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : [];
  var invert = arguments.length > 2 ? arguments[2] : undefined;
  return options.filter(function (_ref3) {
    var id = _ref3.id;
    return invert ? !ids.includes(id) : ids.includes(id);
  });
};

/**
 * Prepend filter name to callback parameters.
 *
 * @param {function} filterName - Filter name
 * @param {function} callback - Callback function
 * @param {function} logger - Logging function
 *
 * @returns {function} callback - Callback function
 */
exports.getOptionsById = getOptionsById;
function prependFilterName(filterName) {
  var callback = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : function () {
    return null;
  };
  var logger = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : function () {
    return null;
  };
  return function (value) {
    logger();
    return callback(filterName, value);
  };
}

/**
 * Sorts results from the `people` query by name since the schema doesn't support it
 *
 * @param {Object} a - first person
 * @param {Object} b - second person
 * @returns {Number} sortResult
 */
function sortPeopleByName(a, b) {
  var aFullName = getFullName(a);
  var bFullName = getFullName(b);
  if (aFullName > bFullName) {
    return 1;
  }
  if (aFullName < bFullName) {
    return -1;
  }
  return 0;
}