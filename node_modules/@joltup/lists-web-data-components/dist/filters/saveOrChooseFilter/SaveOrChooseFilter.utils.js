"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.SAVED_FILTERS = void 0;
exports.convertFiltersToFilterOptions = convertFiltersToFilterOptions;
exports.decode = decode;
exports.fragment = exports.doArrayValuesMatch = exports.deleteReportFilterMutation = void 0;
exports.generateChange = generateChange;
exports.generateOnChangeCallback = generateOnChangeCallback;
exports.generateOnTrailingIconClickCallback = generateOnTrailingIconClickCallback;
exports.getHasChanges = exports.getFormattedDefaultFilterState = exports.getFilterValueFormattedFilters = exports.getCanUpdateSelectedFilter = void 0;
exports.getQueryOptions = getQueryOptions;
exports.updateReportFilterMutation = exports.savedFilterQuery = void 0;
var _client = require("@apollo/client");
var _lodash = require("lodash");
var _utilities = require("../../utilities");
var _templateObject, _templateObject2, _templateObject3;
function _taggedTemplateLiteral(strings, raw) { if (!raw) { raw = strings.slice(0); } return Object.freeze(Object.defineProperties(strings, { raw: { value: Object.freeze(raw) } })); }
function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }
function _iterableToArray(iter) { if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter); }
function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) return _arrayLikeToArray(arr); }
function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i]; return arr2; }
var SAVED_FILTERS = 'SavedFilters';

/**
 * Compare to arrays and see if they are equal (ignoring order)
 *
 * @param {array} array1 First array to compare
 * @param {array} array2 Second array to compare
 * @param {boolean} shouldCheckLength Whether or not to compare array lengths
 *
 * @returns {boolean} If the array values match
 */
exports.SAVED_FILTERS = SAVED_FILTERS;
var doArrayValuesMatch = function doArrayValuesMatch(array1, array2) {
  var shouldCheckLength = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : true;
  if (shouldCheckLength && array1.length !== array2.length) {
    return false;
  }
  var matches = true;
  array1.forEach(function (array1Element) {
    var _ref = array2.find(function (array2Element) {
        return array2Element.name === array1Element.name;
      }) || {},
      _ref$values = _ref.values,
      array2Values = _ref$values === void 0 ? [] : _ref$values;
    if (!(0, _lodash.isEqual)(array1Element.values, array2Values)) {
      matches = false;
      return;
    }
  });
  return matches;
};

/**
 * Convert filters to filter options.
 *
 * @param {array} filters - All the available filters.
 * @param {object} resetOption - The option to reset the report to defaults
 *
 * @returns {array} options - A list options for a dropdown.
 */
exports.doArrayValuesMatch = doArrayValuesMatch;
function convertFiltersToFilterOptions(filters, resetOption) {
  var hasResetOption = !(0, _lodash.isNil)(resetOption) && !(0, _lodash.isEmpty)(resetOption);
  if ((0, _lodash.isNil)(filters)) {
    return hasResetOption ? [resetOption] : [];
  }
  var filterOptions = (0, _lodash.sortBy)(filters.map(function (filter) {
    return {
      displayInApp: filter.displayInApp,
      hoverText: "Report Owner: ".concat(filter.modeOwner.name),
      label: filter.name,
      showIcon: filter.canDelete,
      value: filter.id
    };
  }), function (filter) {
    return filter.label.toLowerCase();
  });
  if (hasResetOption) {
    return [resetOption].concat(_toConsumableArray(filterOptions));
  }
  return _toConsumableArray(filterOptions);
}

/**
 * Decode value if JSON. If not JSON, it returns the original value.
 *
 * @param {any} value - The potential JSON string.
 *
 * @returns {any} value - The JSON decoded value.
 */
function decode(value) {
  try {
    return JSON.parse(value);
  } catch (_unused) {
    return value;
  }
}

/**
 * Convert array of filters to filter/value format
 *
 * @param {array} filters Filters to format
 *
 * @returns {array} Formatted filters
 */
var getFilterValueFormattedFilters = function getFilterValueFormattedFilters(filters) {
  return filters.map(function (field) {
    return {
      filter: field.name,
      value: field.values.map(function (value) {
        return typeof decode(value) === 'boolean' ? value : decode(value);
      })
    };
  });
};

/**
 * Generate a callback function for changing the saved filter.
 *
 * @param {object} params - Parameters
 * @param {string} clamSection - The report type to use for clam.
 * @param {function} setSelectedFilter - Set the Selected filter callback.
 * @param {function} setMultipleFilters - Set multiple filters callback.
 *
 * @returns {function} callback - The function callback.
 */
exports.getFilterValueFormattedFilters = getFilterValueFormattedFilters;
function generateChange(_ref2) {
  var clamSection = _ref2.clamSection,
    _ref2$defaultFilterSt = _ref2.defaultFilterState,
    defaultFilterState = _ref2$defaultFilterSt === void 0 ? [] : _ref2$defaultFilterSt,
    refetch = _ref2.refetch,
    setSelectedFilter = _ref2.setSelectedFilter,
    setMultipleFilters = _ref2.setMultipleFilters;
  return function (filters) {
    _utilities.clam.log(clamSection, SAVED_FILTERS, _utilities.clam.ACTIONS.CHANGE);
    if ((0, _lodash.isNil)(filters) || (0, _lodash.isNil)(filters.id)) {
      setSelectedFilter(null);
      if (!(0, _lodash.isNil)(defaultFilterState)) {
        setMultipleFilters(getFilterValueFormattedFilters(defaultFilterState), false);
      }
      return;
    }

    // set to default values first in case saved filter doesn't have a value
    setMultipleFilters(getFilterValueFormattedFilters(defaultFilterState), false);
    setMultipleFilters(getFilterValueFormattedFilters(filters.fields), true);
    refetch();
    setSelectedFilter(filters.id);
  };
}

/**
 * Generate a callback for filter select onChange
 *
 * @param {object} param - Parameters
 * @param {function} param.onChange - The onChange callback to be called within this callback.
 * @param {array} param.reportFilters - The list of reportFilters that will be searched
 *
 * @returns {function} callback - returns a callback function
 */
function generateOnChangeCallback(_ref3) {
  var onChange = _ref3.onChange,
    reportFilters = _ref3.reportFilters,
    value = _ref3.value;
  if ((0, _lodash.isNil)(value) || (0, _lodash.isNil)(reportFilters)) {
    return onChange(null);
  }
  var chosenFilter = reportFilters.find(function (filter) {
    return filter.id === value;
  });
  onChange(chosenFilter);
  return chosenFilter;
}

/**
 * Generate a callback for tailing icon onclick
 *
 * @param {object} param - Parameters
 * @param {function} param.onClick - The onClick callback to be called within this callback.
 * @param {array} param.reportFilters - The list of reportFilters that will be searched
 *
 * @return {function} returns a callback function
 */
function generateOnTrailingIconClickCallback(_ref4) {
  var setFilterToEdit = _ref4.setFilterToEdit;
  return function (option) {
    return function (e) {
      e.stopPropagation();
      setFilterToEdit(option);
    };
  };
}

/**
 * Returns the options for a graphQL query
 *
 * @param {array} reportTypes - array of ReportType for which to fetch saved filters
 *
 * @returns {object} queryOptions
 */
function getQueryOptions() {
  var reportTypes = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
  return {
    variables: {
      mode: (0, _utilities.getModeInput)(),
      reportTypes: reportTypes
    }
  };
}

/**
 * Get default filter state in array name/values format
 *
 * @param {object} defaultFilterState Default filter state to format
 *
 * @returns {array} Formatted array of name/values
 */
var getFormattedDefaultFilterState = function getFormattedDefaultFilterState(defaultFilterState) {
  if (!(0, _lodash.isNil)(defaultFilterState)) {
    return Object.entries(defaultFilterState).map(function (defaultFilter) {
      return {
        name: defaultFilter[0],
        values: defaultFilter[1].map(function (filter) {
          return _typeof(filter) === 'object' ? JSON.stringify(filter) : filter.toString();
        })
      };
    });
  }
  return defaultFilterState;
};

/**
 * Check to see if there are changes to save, enabling the
 * "Reset Changes" and "Save as New Report" buttons
 *
 * @param {array} currentFilters Current filters to compare
 * @param {string} filterToResetTo Filter to compare
 * @param {array} formattedDefaultFilterState Default state to compare
 * @param {array} reportFilters Report filters to to compare
 * @param {boolean} shouldCheckLength Whether or not to compare array lengths
 *
 * @returns {boolean} True if has changes to save
 */
exports.getFormattedDefaultFilterState = getFormattedDefaultFilterState;
var getHasChanges = function getHasChanges(_ref5) {
  var currentFilters = _ref5.currentFilters,
    filterToResetTo = _ref5.filterToResetTo,
    formattedDefaultFilterState = _ref5.formattedDefaultFilterState,
    reportFilters = _ref5.reportFilters,
    _ref5$shouldCheckLeng = _ref5.shouldCheckLength,
    shouldCheckLength = _ref5$shouldCheckLeng === void 0 ? true : _ref5$shouldCheckLeng;
  if ((0, _lodash.isNil)(formattedDefaultFilterState)) {
    return false;
  }
  if ((0, _lodash.isNil)(filterToResetTo) || filterToResetTo === '') {
    return !doArrayValuesMatch(formattedDefaultFilterState, currentFilters, shouldCheckLength);
  }
  if ((0, _lodash.isNil)(reportFilters)) {
    return false;
  }
  var filters = (0, _lodash.get)(reportFilters.find(function (filter) {
    return filter.id === filterToResetTo;
  }), 'fields');
  if (!(0, _lodash.isNil)(filters)) {
    return !doArrayValuesMatch(filters, currentFilters, shouldCheckLength);
  }
  return false;
};

/**
 * Check to see if user can update a saved report.
 * For example, a content group list should not be editable by a location.
 *
 * @param {string} filterToResetTo id of currently selected report
 * @param {array} reportFilters Saved reports to find selected report
 *
 * @returns {boolean} True if user can update the selected report
 */
exports.getHasChanges = getHasChanges;
var getCanUpdateSelectedFilter = function getCanUpdateSelectedFilter(filterToResetTo, reportFilters) {
  if ((0, _lodash.isNil)(filterToResetTo) || filterToResetTo === '') {
    return true;
  }
  if ((0, _lodash.isNil)(reportFilters)) {
    return false;
  }
  return (0, _lodash.get)(reportFilters.find(function (reportFilter) {
    return reportFilter.id === filterToResetTo;
  }), 'canDelete', false);
};
exports.getCanUpdateSelectedFilter = getCanUpdateSelectedFilter;
var fragment = {
  reportFilter: "\n    id\n    canDelete\n    displayInApp\n    fields {\n      id\n      name\n      values\n    }\n    modeOwner {\n      id\n      name\n    }\n    name\n    reportType\n  "
};
exports.fragment = fragment;
var savedFilterQuery = (0, _client.gql)(_templateObject || (_templateObject = _taggedTemplateLiteral(["\n  query savedReportQuery_SaveOrChooseFilter_Utils(\n    $mode: ModeInput!,\n    $reportTypes: [ReportType]!,\n  ) {\n    reportFilters(mode: $mode, reportTypes: $reportTypes) {\n      ", "\n    }\n  }\n"])), fragment.reportFilter);
exports.savedFilterQuery = savedFilterQuery;
var deleteReportFilterMutation = (0, _client.gql)(_templateObject2 || (_templateObject2 = _taggedTemplateLiteral(["\n  mutation reportFilterMutations_DeleteReportFilter_Mutation($input: DeleteReportFilterInput!) {\n    deleteReportFilter(input: $input) {\n      reportFilter {\n        id\n      }\n    }\n  }\n"])));
exports.deleteReportFilterMutation = deleteReportFilterMutation;
var updateReportFilterMutation = (0, _client.gql)(_templateObject3 || (_templateObject3 = _taggedTemplateLiteral(["\n  mutation reportFilterMutations_UpdateReportFilter_Mutation (\n    $input: UpdateReportFilterInput!\n  ) {\n    updateReportFilter(input: $input) {\n      reportFilter {\n        ", "\n      }\n    }\n  }\n"])), fragment.reportFilter);
exports.updateReportFilterMutation = updateReportFilterMutation;