"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.convertListTemplatesToFilterOptions = convertListTemplatesToFilterOptions;
exports.getUniqueListTemplates = getUniqueListTemplates;
exports.prependFilterName = prependFilterName;
var _lodash = require("lodash");
var _utilities = require("../../utilities");
function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); enumerableOnly && (symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; })), keys.push.apply(keys, symbols); } return keys; }
function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = null != arguments[i] ? arguments[i] : {}; i % 2 ? ownKeys(Object(source), !0).forEach(function (key) { _defineProperty(target, key, source[key]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } return target; }
function _defineProperty(obj, key, value) { key = _toPropertyKey(key); if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }
function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return _typeof(key) === "symbol" ? key : String(key); }
function _toPrimitive(input, hint) { if (_typeof(input) !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (_typeof(res) !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
/**
 * Convert listTemplates to filter options.
 *
 * @param {array} listTemplates - An array of all the available listTemplates.
 *
 * @return {array} options - A list options for a dropdown.
 */
function convertListTemplatesToFilterOptions(listTemplates) {
  if ((0, _lodash.isNil)(listTemplates)) {
    return [];
  }
  return listTemplates.map(function (_ref) {
    var id = _ref.id,
      isActive = _ref.isActive,
      title = _ref.title;
    return _objectSpread({
      id: id,
      isActive: isActive,
      label: title
    }, !isActive && {
      sublabel: 'Inactive'
    });
  });
}

/**
 * Filters listTemplates base on the `plannedCASourceItemTemplateParentId` field to keep triggered CA lists from filling up the filter
 *
 * @param {Array} listTemplates - the listTemplates to filter
 *
 * @returns {Array} listTemplates
 */
function getUniqueListTemplates() {
  var listTemplates = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
  return (0, _lodash.uniqBy)(listTemplates, function (_ref2) {
    var id = _ref2.id,
      plannedCASourceItemTemplateParentId = _ref2.plannedCASourceItemTemplateParentId;
    return plannedCASourceItemTemplateParentId === null ? id : plannedCASourceItemTemplateParentId;
  });
}

/**
 * Prepend filter name to callback parameters.
 *
 * @param {function} callback - Callback function.
 * @param {function} setter - Internal filter state options setter
 * @param {array} options - Current filter state options
 * @param {boolean} isSingleSelection - can the filter only have 1 selectied option
 * @param {function} logger - Logging function.
 *
 * @returns {function} callback - Callback function.
 */
function prependFilterName(callback, setter, options, isSingleSelection) {
  var logger = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : function () {
    return null;
  };
  return function (value) {
    logger();
    return callback(_utilities.FILTER_NAMES.listTemplateIds, setter, options, value, isSingleSelection);
  };
}