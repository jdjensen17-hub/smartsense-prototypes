module.exports = {
  extends: require.resolve("@joltup/web-eslint"),
  overrides: [
    {
      files: ['*.js'],
      rules: {
        "max-len": "off",
      },
    },
  ],
  rules: {
    "sort-keys": ["error", "asc", {caseSensitive: true, natural: true,}]
  }
}

