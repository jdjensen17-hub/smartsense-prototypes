"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.generateLevelChangeCallback = generateLevelChangeCallback;
/**
 *
 * @param {object} param - Parameter
 * @param {object}.{function} setNestedLevel - Setter that keeps track of nestedLevel.
 * @param {object}.{function} setShouldTableRender - Indicates wether or not to render the agGrid.
 * @param {object}.{function} toggleCallback - Callback for parent function when the level changes.
 *
 * @return {function} onChange - select listInstance callback function.
 */
function generateLevelChangeCallback({
  setNestedLevel,
  setShouldTableRender,
  toggleCallback
}) {
  return function callback(level) {
    setNestedLevel(level);
    setShouldTableRender(false);
    toggleCallback(level);
  };
}