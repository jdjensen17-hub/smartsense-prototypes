"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.utilFunctions = exports.default = exports.UP_KEY = exports.FlagsSelector = exports.ENTER_KEY = exports.DOWN_KEY = void 0;
var _classnames = _interopRequireDefault(require("classnames"));
var _lodash = require("lodash");
var _propTypes = _interopRequireDefault(require("prop-types"));
var _react = _interopRequireDefault(require("react"));
var _reactOnclickoutside = _interopRequireDefault(require("react-onclickoutside"));
var _ = require("../");
var _ItemTemplateFlagCondition = require("../itemTemplateFlagCondition/ItemTemplateFlagCondition");
var _FlagsSelector = _interopRequireDefault(require("./FlagsSelector.style"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
const UP_KEY = exports.UP_KEY = 38;
const DOWN_KEY = exports.DOWN_KEY = 40;
const ENTER_KEY = exports.ENTER_KEY = 13;
const utilFunctions = exports.utilFunctions = {
  buildGroupHeaderOption: groupItem => {
    if (!groupItem) {
      return [];
    }
    return [{
      label: `${groupItem.owner.name} Flags`,
      value: groupItem.owner.id,
      isGroupLabel: true
    }];
  }
};
class FlagsSelector extends _react.default.PureComponent {
  constructor(props) {
    super(props);
    this.state = {
      cursorIndex: -1,
      isOpen: false,
      searchText: this.getLabel(props.value) || '',
      shouldScroll: false
    };
  }
  componentDidUpdate(prevProps) {
    const {
      value
    } = this.props;
    if (prevProps.value !== value) {
      const label = this.getLabel();
      this.setState({
        searchText: label
      });
    }
  }
  filterOptionsByLabel(label) {
    let filterLabel = label;
    if ((0, _lodash.isNil)(filterLabel)) {
      filterLabel = '';
    }

    // remove options that don't match
    let filtered = [...this.getTransformedOptions()].filter(option => option.label.toLowerCase().indexOf(filterLabel.toLowerCase()) > -1 || option.isGroupLabel);

    // remove group headers that don't have any matches in the results
    filtered = filtered.filter(option => {
      if (option.isGroupLabel) {
        return filtered.filter(opt => !opt.isGroupLabel && opt.owner.id === option.value).length;
      }
      return true;
    });
    return filtered;
  }
  findOptionByValue(value) {
    const groupedOptions = this.getTransformedOptions();
    const option = groupedOptions.find(opt => opt.value === value);
    if ((0, _lodash.isNil)(option)) {
      return null;
    }
    return option;
  }
  getLabel(value = null) {
    const val = value || this.props.value;
    return (this.findOptionByValue(val) || {}).label;
  }
  getTransformedOptions() {
    const {
      modeInputMode,
      options
    } = this.props;
    const locationOptions = options.filter(anOption => anOption.owner.isLocation);
    const contentGroupOptions = options.filter(anOption => !anOption.owner.isLocation);

    // group each individual content group
    const contentGroupIds = [...new Set(contentGroupOptions.map(option => option.owner.id))];
    const groupedContentGroupItems = [...contentGroupIds.map(id => {
      const contentGroup = contentGroupOptions.filter(option => option.owner.id === id);
      return [...utilFunctions.buildGroupHeaderOption(contentGroup[0]), ...contentGroup];
    })];

    // put all groups together
    return [...(locationOptions.length && modeInputMode !== 'ContentGroup' ? [...utilFunctions.buildGroupHeaderOption(locationOptions[0]), ...locationOptions] : []), ...(groupedContentGroupItems.length ? [].concat(...groupedContentGroupItems) : [])];
  }
  handleClickOption = option => {
    const {
      name,
      onBlur,
      onChange,
      shouldCloseOnChange
    } = this.props;
    const label = this.getLabel();
    this.setState({
      searchText: label
    });
    onChange(name, {
      type: option.selected ? _ItemTemplateFlagCondition.FLAG_CHANGE_TYPES.DELETE : _ItemTemplateFlagCondition.FLAG_CHANGE_TYPES.ADD,
      id: option.value
    });
    if (shouldCloseOnChange) {
      this.setState({
        isOpen: false
      });
      onBlur?.();
    }
  };
  handleClickOutside() {
    const label = this.getLabel();
    this.setState({
      cursorIndex: -1,
      isOpen: false,
      searchText: label
    });
    this.props.onBlur();
  }
  handleCustom = () => {
    const {
      searchText
    } = this.state;
    const {
      name,
      onCustom
    } = this.props;
    onCustom(name, searchText);
  };
  handleFocus = () => {
    this.setState({
      searchText: '',
      isOpen: true
    });
    this.props.onFocus();
  };
  handleKeyDown = event => {
    const {
      cursorIndex
    } = this.state;
    const groupedOptions = this.getTransformedOptions();
    const keyCode = event.which;
    let newCursorIndex = cursorIndex;
    switch (keyCode) {
      case UP_KEY:
        newCursorIndex -= 1;
        if (groupedOptions[newCursorIndex].isGroupLabel) {
          newCursorIndex -= 1;
        }
        break;
      case DOWN_KEY:
        newCursorIndex += 1;
        if (groupedOptions[newCursorIndex].isGroupLabel) {
          newCursorIndex += 1;
        }
        break;
      case ENTER_KEY:
        if (cursorIndex === -1) {
          this.handleCustom();
        } else {
          this.handleClickOption(groupedOptions[cursorIndex]);
        }
        break;
      default:
        newCursorIndex = -1;
    }
    if (newCursorIndex === cursorIndex || newCursorIndex > groupedOptions.length - 1 || newCursorIndex < -1) {
      return null;
    }
    this.setState({
      cursorIndex: newCursorIndex,
      shouldScroll: true
    });
    return null;
  };
  handleTextChange = (name, search) => {
    this.setState({
      searchText: search
    });
  };
  setRef = ref => {
    this.inputRef = ref;
    ref && ref.focus();
    this.props.setRef(ref);
  };
  renderCustomPrompt(groupedOptions) {
    const {
      cursorIndex,
      shouldScroll
    } = this.state;
    const {
      customLabel,
      showCustomPrompt
    } = this.props;
    if (!showCustomPrompt) {
      return null;
    }
    const classes = (0, _classnames.default)({
      'custom-prompt': true,
      'cursor-over': groupedOptions.length === 0 || cursorIndex === -1
    });

    /* istanbul ignore next */
    const scrollRef = node => {
      if (node !== null && cursorIndex === -1 && shouldScroll) {
        this.setState({
          shouldScroll: false
        });
        node.scrollIntoViewIfNeeded && node.scrollIntoViewIfNeeded();
      }
    };
    return /*#__PURE__*/_react.default.createElement("div", {
      className: classes,
      onClick: this.handleCustom,
      onMouseMove: this.handleOptionHover(-1),
      ref: scrollRef
    }, /*#__PURE__*/_react.default.createElement("i", {
      className: "icon-settings1"
    }), /*#__PURE__*/_react.default.createElement("span", null, customLabel), /*#__PURE__*/_react.default.createElement("div", {
      className: "bottom-border"
    }));
  }
  handleOptionHover = index => () => {
    if (this.state.cursorIndex !== index) {
      this.setState({
        cursorIndex: index
      });
    }
  };
  renderOptions(searchText) {
    const {
      isOpen
    } = this.state;
    const {
      showCustomPrompt
    } = this.props;
    const groupedOptions = this.filterOptionsByLabel(searchText);
    if (!isOpen) {
      return null;
    }
    const optionsClasses = (0, _classnames.default)({
      options: true,
      'has-custom-prompt': showCustomPrompt
    });
    return /*#__PURE__*/_react.default.createElement("div", {
      className: optionsClasses
    }, this.renderCustomPrompt(groupedOptions), this.renderOptionItems(groupedOptions));
  }
  renderOptionItems(groupedOptions) {
    const {
      cursorIndex,
      shouldScroll
    } = this.state;
    const {
      isDisabled
    } = this.props;
    if (groupedOptions.length === 0) {
      return /*#__PURE__*/_react.default.createElement("div", {
        className: "option no-results"
      }, "No options found");
    }
    return groupedOptions.map((option, index) => {
      const classes = (0, _classnames.default)({
        'cursor-over': index === cursorIndex,
        'group-header': option.isGroupLabel,
        option: !option.isGroupLabel
      });
      const handleClickOption = e => {
        // preventDefault is so that clicks on the child checkbox don't trigger a second event
        e.preventDefault();
        !groupedOptions[index].isGroupLabel && this.handleClickOption(option);
      };

      /* istanbul ignore next */
      const scrollRef = node => {
        if (node !== null && index === cursorIndex && shouldScroll) {
          this.setState({
            shouldScroll: false
          });
          node.scrollIntoViewIfNeeded && node.scrollIntoViewIfNeeded();
        }
      };
      return /*#__PURE__*/_react.default.createElement("div", {
        className: classes,
        key: option.value,
        onClick: handleClickOption,
        onMouseMove: this.handleOptionHover(index),
        ref: scrollRef
      }, option.isGroupLabel && index !== 0 && /*#__PURE__*/_react.default.createElement("div", {
        className: "top-border"
      }), !option.isGroupLabel && /*#__PURE__*/_react.default.createElement(_.Field, {
        inputName: "flagSelectorCheckbox",
        isDisabled: isDisabled,
        type: "checkbox",
        value: option.selected
      }), /*#__PURE__*/_react.default.createElement("span", null, option.label));
    });
  }
  render() {
    const {
      children,
      hasError,
      isDisabled,
      isRequired,
      label,
      name
    } = this.props;
    const {
      isOpen,
      searchText
    } = this.state;
    const classes = (0, _classnames.default)({
      select: true,
      'is-open': isOpen
    });
    const handleSelectorClick = () => {
      if (!isDisabled && !isOpen) {
        this.handleFocus();
        this.setState({
          isOpen: true
        });
      }
    };
    return /*#__PURE__*/_react.default.createElement(_FlagsSelector.default, {
      className: classes,
      onClick: handleSelectorClick,
      onKeyDown: this.handleKeyDown
    }, /*#__PURE__*/_react.default.createElement("div", {
      className: (0, _classnames.default)({
        'chip-input': true,
        focus: isOpen
      })
    }, children, isOpen && /*#__PURE__*/_react.default.createElement(_.Input, {
      autofocus: true,
      hasError: hasError,
      isDisabled: isDisabled,
      isRequired: isRequired,
      name: name,
      onChange: this.handleTextChange,
      setRef: this.setRef,
      shouldAssignNameAttribute: false,
      shouldAutoComplete: false,
      testid: "flagSelector",
      value: searchText
    })), /*#__PURE__*/_react.default.createElement(_.Label, {
      inputName: name,
      isRequired: isRequired
    }, label), /*#__PURE__*/_react.default.createElement("i", {
      className: "list-triangle-down"
    }), this.renderOptions(searchText));
  }
}
exports.FlagsSelector = FlagsSelector;
FlagsSelector.propTypes = {
  children: _propTypes.default.any,
  customLabel: _propTypes.default.string,
  grouped: _propTypes.default.bool,
  hasError: _propTypes.default.bool,
  isDisabled: _propTypes.default.bool,
  isRequired: _propTypes.default.bool,
  label: _propTypes.default.string,
  modeInputMode: _propTypes.default.string,
  name: _propTypes.default.string.isRequired,
  onBlur: _propTypes.default.func,
  onChange: _propTypes.default.func,
  onCustom: _propTypes.default.func,
  onFocus: _propTypes.default.func,
  options: _propTypes.default.arrayOf(_propTypes.default.shape({
    label: _propTypes.default.string,
    owner: _propTypes.default.shape({
      id: _propTypes.default.string,
      isLocation: _propTypes.default.bool,
      name: _propTypes.default.string
    }),
    selected: _propTypes.default.bool,
    value: _propTypes.default.string
  })),
  setRef: _propTypes.default.func,
  shouldCloseOnChange: _propTypes.default.bool,
  showCustomPrompt: _propTypes.default.bool,
  value: _propTypes.default.string
};
FlagsSelector.defaultProps = {
  grouped: false,
  hasError: false,
  isDisabled: false,
  isRequired: false,
  modeInputMode: '',
  onBlur: Function.prototype,
  onChange: Function.prototype,
  onCustom: Function.prototype,
  onFocus: Function.prototype,
  options: [],
  setRef: Function.prototype,
  showCustomPrompt: false
};
var _default = exports.default = (0, _reactOnclickoutside.default)(FlagsSelector);