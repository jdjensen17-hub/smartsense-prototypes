"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = exports.UP_KEY = exports.MultiSelect = exports.ENTER_KEY = exports.DOWN_KEY = void 0;
var _classnames = _interopRequireDefault(require("classnames"));
var _lodash = require("lodash");
var _propTypes = _interopRequireDefault(require("prop-types"));
var _react = _interopRequireDefault(require("react"));
var _reactOnclickoutside = _interopRequireDefault(require("react-onclickoutside"));
var _ = require("../");
var _loadingSpinner = _interopRequireDefault(require("../loadingSpinner"));
var _Select = require("../select/Select.utils");
var _MultiSelect = _interopRequireDefault(require("./MultiSelect.style"));
var _MultiSelect2 = require("./MultiSelect.utils");
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
const UP_KEY = exports.UP_KEY = 38;
const DOWN_KEY = exports.DOWN_KEY = 40;
const ENTER_KEY = exports.ENTER_KEY = 13;
class MultiSelect extends _react.default.PureComponent {
  constructor(props) {
    super(props);
    this.state = {
      cursorIndex: -1,
      filteredOptions: props.shouldHideSelectedOptions ? this.filterOptionsBySelected(props.options) : props.options,
      isOpen: false,
      searchText: this.getLabel(props.value) || ''
    };
  }
  componentDidUpdate(prevProps) {
    const {
      options,
      shouldHideSelectedOptions,
      value
    } = this.props;
    if (prevProps.value !== value) {
      const label = this.getLabel();
      this.setState({
        searchText: label
      });
      if (shouldHideSelectedOptions) {
        this.setState({
          filteredOptions: this.filterOptionsBySelected(options)
        });
      }
    }
    if ((0, _Select.haveOptionsUpdated)(prevProps.options, options)) {
      this.setState({
        filteredOptions: this.filterOptionsByLabel(this.state.searchText)
      });
    }
  }
  filterOptionsByLabel(label = '') {
    const {
      options,
      shouldHideSelectedOptions
    } = this.props;
    const filteredByLabel = options.filter(option => option.label.toLowerCase().indexOf(label.toLowerCase()) > -1);
    return shouldHideSelectedOptions ? this.filterOptionsBySelected(filteredByLabel) : filteredByLabel;
  }
  filterOptionsBySelected(options) {
    const {
      value
    } = this.props;
    return options.filter(option => !Object.keys(value).includes(option.value));
  }
  findOptionByValue(value) {
    const {
      options
    } = this.props;
    for (let index = 0; index < options.length; index++) {
      if (options[index].value === value) {
        return options[index];
      }
    }
    return null;
  }
  getLabel(value = null) {
    const val = value || this.props.value;
    return (this.findOptionByValue(val) || {}).label;
  }
  handleClickOption = option => {
    const {
      name,
      onChange,
      value
    } = this.props;
    const label = this.getLabel();
    this.setState({
      cursorIndex: -1,
      searchText: label
    });
    const isAdd = !Object.hasOwn(value, option.value);
    onChange(name, (0, _MultiSelect2.buildOnChangeValue)(isAdd, option));
    this.inputRef.focus();
  };
  handleClickOutside() {
    const label = this.getLabel();
    this.setState({
      cursorIndex: -1,
      isOpen: false,
      searchText: label
    });
    this.props.onBlur();
  }
  handleCustom = () => {
    const {
      searchText
    } = this.state;
    const {
      name,
      onCustom
    } = this.props;
    onCustom(name, searchText);
  };
  handleFocus = () => {
    const {
      options,
      shouldHideSelectedOptions
    } = this.props;
    this.setState({
      cursorIndex: -1,
      filteredOptions: shouldHideSelectedOptions ? this.filterOptionsBySelected(options) : options,
      searchText: '',
      isOpen: true
    });
    this.props.onFocus();
    this.inputRef.focus();
  };
  handleKeyDown = event => {
    const {
      cursorIndex,
      filteredOptions
    } = this.state;
    const keyCode = event.which;
    let newCursorIndex = cursorIndex;
    switch (keyCode) {
      case UP_KEY:
        newCursorIndex -= 1;
        break;
      case DOWN_KEY:
        newCursorIndex += 1;
        break;
      case ENTER_KEY:
        if (cursorIndex === -1) {
          this.handleCustom();
        } else {
          this.handleClickOption(filteredOptions[cursorIndex]);
        }
        break;
      default:
        newCursorIndex = -1;
    }
    if (newCursorIndex === cursorIndex || newCursorIndex > filteredOptions.length - 1 || newCursorIndex < -1) {
      return null;
    }
    this.setState({
      cursorIndex: newCursorIndex
    });
    return null;
  };
  handleSelectorClick = e => {
    const {
      isDisabled
    } = this.props;
    const {
      isOpen
    } = this.state;
    if (e.target.tagName === 'I') {
      e.preventDefault();
      return;
    }
    if (!isDisabled && !isOpen) {
      this.handleFocus();
    }
  };
  handleTextChange = (name, search) => {
    this.setState({
      filteredOptions: this.filterOptionsByLabel(search),
      searchText: search
    });
  };
  setRef = ref => {
    this.inputRef = ref;
    this.props.shouldAutoFocus && ref?.focus();
    this.props.setRef(ref);
  };
  renderChips() {
    const {
      constrainLimit,
      constrainMessage,
      constrainSelections,
      isDisabled,
      onChange,
      testid,
      value
    } = this.props;
    const {
      isOpen
    } = this.state;
    const chips = [];
    const buildChip = (data, key) => {
      const deleteCallback = (0, _MultiSelect2.generateDeleteChipCallback)({
        key,
        onChange
      });
      return /*#__PURE__*/_react.default.createElement(_.Chip, {
        key: key,
        onDelete: deleteCallback,
        showDelete: !isDisabled,
        testid: `chip-${testid}`,
        text: (0, _lodash.truncate)(data.label, {
          length: 20
        })
      });
    };
    (0, _lodash.forIn)(value, (data, key) => {
      chips.push(buildChip(data, key));
    });
    if (!isOpen && constrainSelections && Object.keys(value).length > Math.max(0, constrainLimit)) {
      const constrainedChips = chips.slice(0, Math.max(0, constrainLimit));
      const extraItems = chips.length - constrainedChips.length;
      const classes = (0, _classnames.default)({
        'constrain-message': true
      });
      return /*#__PURE__*/_react.default.createElement(_react.default.Fragment, null, constrainedChips, /*#__PURE__*/_react.default.createElement("span", {
        className: classes
      }, constrainMessage(extraItems)));
    }
    return chips;
  }
  renderCustomPrompt() {
    const {
      filteredOptions
    } = this.state;
    const {
      customIconName,
      customLabel,
      showCustomPrompt
    } = this.props;
    if (!showCustomPrompt) {
      return null;
    }
    const classes = (0, _classnames.default)({
      'custom-prompt': true,
      'cursor-over': filteredOptions.length === 0
    });
    return /*#__PURE__*/_react.default.createElement("div", {
      className: classes,
      onClick: this.handleCustom
    }, /*#__PURE__*/_react.default.createElement("i", {
      className: customIconName
    }), /*#__PURE__*/_react.default.createElement("span", null, customLabel));
  }
  renderOptions() {
    const {
      isOpen
    } = this.state;
    const {
      showCustomPrompt,
      isLoading
    } = this.props;
    if (!isOpen) {
      return null;
    }
    if (isLoading) {
      return /*#__PURE__*/_react.default.createElement("div", {
        className: "options"
      }, /*#__PURE__*/_react.default.createElement("div", {
        className: "loading-wrapper"
      }, /*#__PURE__*/_react.default.createElement(_loadingSpinner.default, null)));
    }
    const optionsClasses = (0, _classnames.default)({
      options: true,
      'has-custom-prompt': showCustomPrompt
    });
    return /*#__PURE__*/_react.default.createElement("div", {
      className: optionsClasses
    }, this.renderCustomPrompt(), this.renderOptionItems());
  }
  renderOptionItems() {
    const {
      groupBy
    } = this.props;
    const {
      filteredOptions
    } = this.state;
    if (filteredOptions.length === 0) {
      return /*#__PURE__*/_react.default.createElement("div", {
        className: "option no-results"
      }, "No options found");
    }
    if (groupBy) {
      return /*#__PURE__*/_react.default.createElement("div", {
        className: "option-groups"
      }, this.renderOptionGroup((0, _MultiSelect2.getGroupedOptions)(filteredOptions, groupBy)));
    }
    return this.renderOptionSet(filteredOptions);
  }
  renderOptionGroup(optionGroups) {
    return optionGroups.map(({
      key,
      options
    }) => /*#__PURE__*/_react.default.createElement("div", {
      className: "option-group",
      key: key
    }, /*#__PURE__*/_react.default.createElement("div", {
      className: "group-header"
    }, /*#__PURE__*/_react.default.createElement("span", null, key)), this.renderOptionSet(options)));
  }
  renderOptionSet(options) {
    const {
      shouldHideSelectedOptions,
      value
    } = this.props;
    const {
      cursorIndex
    } = this.state;
    return options.map((option, index) => {
      const classes = (0, _classnames.default)({
        option: true,
        'cursor-over': index === cursorIndex,
        'hide-checkbox': shouldHideSelectedOptions
      });
      return /*#__PURE__*/_react.default.createElement("div", {
        className: classes,
        key: index,
        onClick: this.handleClickOption.bind(this, option)
      }, /*#__PURE__*/_react.default.createElement(_.Field, {
        inputName: "multiSelectorCheckbox",
        label: option.label,
        testid: `multiselect-option-${index}`,
        type: "checkbox",
        value: Object.hasOwn(value, option.value)
      }));
    });
  }
  render() {
    const {
      compact,
      constrainLimit,
      constrainSelections,
      hasError,
      isDisabled,
      isRequired,
      label,
      name,
      parentId,
      shouldAutoFocus,
      testid
    } = this.props;
    const {
      isOpen,
      searchText
    } = this.state;
    const multiSelectClasses = (0, _classnames.default)({
      multiselect: true,
      compact,
      'fully-constrained': constrainSelections && constrainLimit <= 0,
      'is-open': isOpen
    });
    const inputClasses = (0, _classnames.default)({
      'chip-input': true,
      focus: isOpen
    });
    const chips = this.renderChips();
    return /*#__PURE__*/_react.default.createElement(_MultiSelect.default, {
      className: multiSelectClasses,
      onClick: this.handleSelectorClick,
      onKeyDown: this.handleKeyDown
    }, /*#__PURE__*/_react.default.createElement("div", {
      className: inputClasses
    }, chips, /*#__PURE__*/_react.default.createElement(_.Input, {
      autofocus: shouldAutoFocus,
      forceHasValue: chips.length > 0 || /*#__PURE__*/_react.default.isValidElement(chips),
      hasError: hasError,
      isDisabled: isDisabled,
      isRequired: isRequired,
      name: name,
      onChange: this.handleTextChange,
      parentId: parentId,
      setRef: this.setRef,
      shouldAssignNameAttribute: false,
      shouldAutoComplete: false,
      shouldBlurOnEnter: false,
      testid: `input-${testid}`,
      value: searchText
    }), /*#__PURE__*/_react.default.createElement(_.Label, {
      inputName: name,
      isRequired: isRequired,
      parentId: parentId,
      type: 'input'
    }, label)), /*#__PURE__*/_react.default.createElement("i", {
      className: "list-triangle-down"
    }), this.renderOptions());
  }
}
exports.MultiSelect = MultiSelect;
MultiSelect.propTypes = {
  compact: _propTypes.default.bool,
  constrainLimit: _propTypes.default.number,
  // Needs constrainSelections to be true
  constrainMessage: _propTypes.default.func,
  // Needs constrainSelections to be true
  constrainSelections: _propTypes.default.bool,
  customIconName: _propTypes.default.string,
  customLabel: _propTypes.default.string,
  groupBy: _propTypes.default.func,
  hasError: _propTypes.default.bool,
  isDisabled: _propTypes.default.bool,
  isLoading: _propTypes.default.bool,
  isRequired: _propTypes.default.bool,
  label: _propTypes.default.string,
  name: _propTypes.default.string.isRequired,
  onBlur: _propTypes.default.func,
  onChange: _propTypes.default.func,
  onCustom: _propTypes.default.func,
  onFocus: _propTypes.default.func,
  options: _propTypes.default.arrayOf(_propTypes.default.shape({
    isDisabled: _propTypes.default.bool,
    label: _propTypes.default.string,
    value: _propTypes.default.string
  })),
  parentId: _propTypes.default.oneOfType([_propTypes.default.number, _propTypes.default.string]),
  setRef: _propTypes.default.func,
  shouldAutoFocus: _propTypes.default.bool,
  shouldHideSelectedOptions: _propTypes.default.bool,
  showCustomPrompt: _propTypes.default.bool,
  testid: _propTypes.default.string,
  /** value can have as many keys as are being supplied as selected.
   * The key would be the value prop of a matching option */
  value: _propTypes.default.shape({
    value: _propTypes.default.shape({
      label: _propTypes.default.string
    })
  })
};
MultiSelect.defaultProps = {
  compact: false,
  constrainLimit: 1,
  constrainMessage: extraItems => `+ ${extraItems} more selected`,
  constrainSelections: false,
  customIconName: 'list-plus-circle',
  groupBy: null,
  hasError: false,
  isDisabled: false,
  isRequired: false,
  onBlur: Function.prototype,
  onChange: Function.prototype,
  onCustom: Function.prototype,
  onFocus: Function.prototype,
  options: [],
  setRef: Function.prototype,
  shouldHideSelectedOptions: false,
  showCustomPrompt: false,
  value: Object.prototype
};
var _default = exports.default = (0, _reactOnclickoutside.default)(MultiSelect);