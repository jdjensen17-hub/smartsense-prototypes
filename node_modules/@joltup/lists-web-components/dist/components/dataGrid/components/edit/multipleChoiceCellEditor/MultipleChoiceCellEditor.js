"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.MultipleChoiceCellEditor = void 0;
var _react = _interopRequireWildcard(require("react"));
var _select = _interopRequireDefault(require("../../../../select"));
var _MultipleChoiceCellEditor = require("./MultipleChoiceCellEditor.style");
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _interopRequireWildcard(e, t) { if ("function" == typeof WeakMap) var r = new WeakMap(), n = new WeakMap(); return (_interopRequireWildcard = function (e, t) { if (!t && e && e.__esModule) return e; var o, i, f = { __proto__: null, default: e }; if (null === e || "object" != typeof e && "function" != typeof e) return f; if (o = t ? n : r) { if (o.has(e)) return o.get(e); o.set(e, f); } for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]); return f; })(e, t); }
const MultipleChoiceCellEditor = ({
  itemOptions,
  name,
  value,
  ...agGridProps
}) => {
  const [focused, setFocused] = (0, _react.useState)(false);

  // We use this to determine if the select dropdown should render above or below the cell
  const agGridRoot = document.querySelector('.ag-root');
  const cellBox = (0, _react.useMemo)(() => agGridProps.eGridCell.getBoundingClientRect(), [agGridProps.eGridCell]);
  const options = (0, _react.useMemo)(() => itemOptions.map(itemOption => ({
    label: itemOption.value,
    value: itemOption.parentId
  })), [itemOptions]);
  return /*#__PURE__*/_react.default.createElement(_MultipleChoiceCellEditor.MultipleChoiceCellEditorStyle, {
    className: "data-grid-multiple-choice-cell-editor",
    style: {
      // The subtractions are to account for borders
      width: `${cellBox.width - 2}px`,
      height: `${cellBox.height - 1}px`
    }
  }, /*#__PURE__*/_react.default.createElement(_select.default, {
    name: name,
    onBlur: () => {
      // This timeout is needed so that React has time to set the state before the grid tries to stop editing
      setTimeout(() => {
        agGridProps.stopEditing();
      });
    },
    onChange: (_name, newValue) => {
      const newItemOption = itemOptions.find(itemOption => itemOption.parentId === newValue);
      if (!newItemOption) {
        throw new Error('MultipleChoiceCellEditor: Option not found when selecting');
      }
      agGridProps.onValueChange([newItemOption]);
      // This timeout is needed so that React has time to set the state before the grid tries to stop editing
      setTimeout(() => {
        agGridProps.stopEditing();
      });
    },
    options: options,
    overflowContainer: agGridRoot,
    setRef: inputRef => {
      if (!focused) {
        inputRef?.focus();
        setFocused(true);
      }
    },
    value: value?.[0]?.parentId ?? null
  }));
};
exports.MultipleChoiceCellEditor = MultipleChoiceCellEditor;
MultipleChoiceCellEditor.displayName = 'SelectDropdown';