"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = exports.MultiSelectDropdown = void 0;
var _react = _interopRequireWildcard(require("react"));
var _index = require("../index");
var _MultiSelectDropdown = require("./MultiSelectDropdown.style");
function _interopRequireWildcard(e, t) { if ("function" == typeof WeakMap) var r = new WeakMap(), n = new WeakMap(); return (_interopRequireWildcard = function (e, t) { if (!t && e && e.__esModule) return e; var o, i, f = { __proto__: null, default: e }; if (null === e || "object" != typeof e && "function" != typeof e) return f; if (o = t ? n : r) { if (o.has(e)) return o.get(e); o.set(e, f); } for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]); return f; })(e, t); }
const MultiSelectDropdown = ({
  inputRef,
  isLoading = false,
  maxHeight = 500,
  onChange,
  onClose,
  options,
  optionsHeader,
  placeholder,
  readonly = false,
  root = null,
  searchPlaceholder = 'Search',
  selected = []
}) => {
  const [searchString, setSearchString] = (0, _react.useState)('');
  const [isOpen, setIsOpen] = (0, _react.useState)(false);
  const [reverseOptionsBox, setReverseOptionsBox] = _react.default.useState(false);
  const ref = _react.default.useRef(null);
  (0, _react.useEffect)(() => {
    const handleClickOutside = event => {
      if (ref.current && !ref.current.contains(event.target)) {
        if (onClose) {
          onClose();
        }
        setIsOpen(false);
      }
    };
    document.addEventListener('click', handleClickOutside);
    return () => {
      document.removeEventListener('click', handleClickOutside);
    };
  }, [onClose]);
  (0, _react.useEffect)(() => {
    const inputBox = ref?.current?.getBoundingClientRect();
    const overflowContainerBox = root?.getBoundingClientRect();
    if (!inputBox || !overflowContainerBox) {
      return;
    }
    const bottomSpace = overflowContainerBox.bottom - inputBox.bottom;
    if (bottomSpace < maxHeight) {
      setReverseOptionsBox(true);
    } else if (bottomSpace > maxHeight && reverseOptionsBox) {
      setReverseOptionsBox(false);
    }
  }, []);
  const filteredOptions = (0, _react.useMemo)(() => {
    if (options.length > 0 && 'options' in options[0]) {
      // Handle OptionGroup[]
      return options.map(group => ({
        ...group,
        options: group.options.filter(option => option.label.toLowerCase().includes(searchString?.toLowerCase() ?? ''))
      }));
    }

    // Handle Option[]
    return options.filter(option => option.label.toLowerCase().includes(searchString?.toLowerCase() ?? ''));
  }, [searchString, options]);
  const sortedOptions = (0, _react.useMemo)(() => {
    const selectedIds = selected.map(option => option.id);
    const sortFn = (a, b) => {
      const aSelected = selectedIds.includes(a.id);
      const bSelected = selectedIds.includes(b.id);
      if (aSelected && !bSelected) return -1;
      if (!aSelected && bSelected) return 1;
      return a.label.localeCompare(b.label, undefined, {
        numeric: true,
        sensitivity: 'base'
      });
    };
    if (options.length > 0 && 'options' in options[0]) {
      // Handle OptionGroup[]
      return filteredOptions.map(group => ({
        ...group,
        options: group.options.sort(sortFn)
      }));
    }

    // Handle Option[]
    return filteredOptions.sort(sortFn);
  }, [filteredOptions, options, selected]);
  const sortedSelectedOptions = (0, _react.useMemo)(() => selected.sort((a, b) => a.label.localeCompare(b.label, undefined, {
    numeric: true,
    sensitivity: 'base'
  })), [selected]);
  return /*#__PURE__*/_react.default.createElement(_MultiSelectDropdown.MultiSelectDropdownStyle, {
    className: "multi-select-dropdown",
    ref: ref
  }, !isOpen && /*#__PURE__*/_react.default.createElement("div", {
    className: "multi-select-dropdown-closed"
  }, /*#__PURE__*/_react.default.createElement(_index.Input, {
    isDisabled: readonly,
    name: "selected-options",
    onFocus: () => !readonly && setIsOpen(true),
    placeholder: placeholder,
    setRef: inputRef,
    testid: "",
    value: sortedSelectedOptions.map(option => option.label).join(', ') || ''
  }), /*#__PURE__*/_react.default.createElement("i", {
    className: "list-triangle-down"
  }), isLoading && /*#__PURE__*/_react.default.createElement("div", {
    className: "loading-container"
  }, /*#__PURE__*/_react.default.createElement(_index.LoadingSpinner, null))), isOpen && /*#__PURE__*/_react.default.createElement("div", {
    className: "multi-select-dropdown-open"
  }, /*#__PURE__*/_react.default.createElement(_index.Input, {
    name: "option-filter",
    onChange: (_name, newValue) => {
      setSearchString(newValue);
    },
    placeholder: searchPlaceholder,
    testid: "",
    value: searchString
  }), /*#__PURE__*/_react.default.createElement("div", {
    className: `options-dropdown ${reverseOptionsBox ? 'reverse' : ''}`,
    style: {
      maxHeight: `${maxHeight}px`
    }
  }, optionsHeader, sortedOptions.map(optionOrGroup => {
    // Handle OptionGroup
    if ('options' in optionOrGroup) {
      if (optionOrGroup.options.length > 0) {
        return /*#__PURE__*/_react.default.createElement("div", {
          className: "option-group",
          key: optionOrGroup.label
        }, /*#__PURE__*/_react.default.createElement("div", {
          className: "group-label"
        }, optionOrGroup.label), optionOrGroup.options.map(option => /*#__PURE__*/_react.default.createElement("div", {
          className: "option-container",
          key: option.id
        }, /*#__PURE__*/_react.default.createElement(_index.Checkbox, {
          isChecked: selected.some(v => v.id === option.id),
          label: option.label,
          name: option.label,
          onChange: (_, isChecked) => {
            const newSelectedOptions = isChecked ? [...selected, option] : selected.filter(selectedOption => selectedOption.id !== option.id);
            onChange(newSelectedOptions);
          }
        }))));
      }
      return null;
    }

    // Handle Option
    return /*#__PURE__*/_react.default.createElement("div", {
      className: "option-container",
      key: optionOrGroup.id
    }, /*#__PURE__*/_react.default.createElement(_index.Checkbox, {
      isChecked: selected.some(v => v.id === optionOrGroup.id),
      label: optionOrGroup.label,
      name: optionOrGroup.label,
      onChange: (_, isChecked) => {
        const newSelectedOptions = isChecked ? [...selected, optionOrGroup] : selected.filter(selectedOption => selectedOption.id !== optionOrGroup.id);
        onChange(newSelectedOptions);
      }
    }));
  }))));
};
exports.MultiSelectDropdown = MultiSelectDropdown;
var _default = exports.default = MultiSelectDropdown;