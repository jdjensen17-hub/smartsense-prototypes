"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = exports.MAX_SHOWN_FLAGS = exports.MAX_CHARACTERS = void 0;
exports.lengthValidator = lengthValidator;
var _listsCore = require("@joltup/lists-core");
var _immutable = require("immutable");
var _lodash = require("lodash");
var _propTypes = _interopRequireDefault(require("prop-types"));
var _react = _interopRequireDefault(require("react"));
var _reactImmutableProptypes = _interopRequireDefault(require("react-immutable-proptypes"));
var _runes = _interopRequireDefault(require("runes"));
var _2 = require("../");
var _FlagsDialog = _interopRequireDefault(require("./FlagsDialog.style"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
const MAX_CHARACTERS = exports.MAX_CHARACTERS = 'Max 3 characters';
const MAX_SHOWN_FLAGS = exports.MAX_SHOWN_FLAGS = 200;
function lengthValidator({
  errors,
  object
}) {
  if ((0, _lodash.isNil)(object)) {
    return errors;
  }
  const symbolValue = _immutable.Map.isMap(object) ? object.get('symbol') : object.symbol;

  // Supports emoji for emojis that are technically more than 1 character
  if ((0, _runes.default)(symbolValue).length > 3) {
    errors.push(MAX_CHARACTERS);
  }
  return errors;
}
const validators = Object.freeze({
  color: [],
  name: [_listsCore.Validators.OPTIONS.required],
  symbol: [_listsCore.Validators.OPTIONS.required, lengthValidator]
});
class FlagsDialog extends _react.default.PureComponent {
  constructor(props) {
    super(props);
    this.state = {
      flags: props.flags.map(flag => (0, _immutable.fromJS)({
        data: flag,
        errors: {},
        isDirty: false
      })),
      hasNew: false,
      isLoading: false,
      searchString: '',
      showAll: props.flags.size < MAX_SHOWN_FLAGS
    };
  }
  addFlag = () => {
    const {
      flags
    } = this.state;
    const newFlag = (0, _immutable.fromJS)({
      data: {
        color: '000000FF',
        id: `temporary${Date.now()}`,
        isActive: true,
        name: '',
        symbol: ''
      },
      errors: {},
      isDirty: false
    });
    this.setState({
      flags: flags.unshift(newFlag),
      hasNew: true
    });
  };
  deactivate = id => {
    const {
      flags
    } = this.state;
    const index = flags.findIndex(flag => flag.getIn(['data', 'id']) === id);
    if (flags.getIn([index, 'data', 'id']).includes('temporary')) {
      this.setState({
        flags: flags.delete(index)
      });
    } else {
      this.setState({
        flags: flags.update(index, flag => flag.mergeDeep({
          data: {
            isActive: false,
            activated: false
          },
          isDirty: true
        }))
      });
    }
  };
  activate = id => {
    const {
      flags
    } = this.state;
    const index = flags.findIndex(flag => flag.getIn(['data', 'id']) === id);
    this.setState({
      flags: flags.update(index, flag => {
        const isAlreadyActive = flag.getIn(['data', 'isActive']);
        return flag.mergeDeep({
          data: {
            activated: !isAlreadyActive,
            isActive: true
          },
          isDirty: true
        });
      })
    });
  };
  handleChange = (inputName, value) => {
    const {
      flags
    } = this.state;
    const [id, type, field] = inputName.split('.');
    const index = flags.findIndex(flag => flag.getIn(['data', 'id']) === id);
    const object = flags.getIn([index, type]).set(field, value).set('updated', true);
    const errors = _listsCore.Validators.validate(validators[field], value, object);
    this.setState({
      flags: flags.setIn([index, type], object).setIn([index, 'errors', field], errors).setIn([index, 'isDirty'], true)
    });
  };
  handleColorChange = (inputName, value) => {
    this.handleChange(inputName, value.substring(1).concat('FF'));
  };
  handleSave = () => {
    let {
      flags
    } = this.state;
    const itemsToSave = [];
    let errorsFound = false;
    flags.forEach((flag, index) => {
      if (flag.get('isDirty')) {
        const errors = {};
        Object.keys(validators).forEach(key => {
          const attributeErrors = _listsCore.Validators.validate(validators[key], flag.getIn(['data', key]), flag.get('data'));
          if (!(0, _lodash.isEmpty)(attributeErrors)) {
            errors[key] = attributeErrors;
            errorsFound = true;
          }
        });
        if ((0, _lodash.isEmpty)(errors)) {
          let theFlag = flag;
          if (flag.getIn(['data', 'id']).includes('temporary')) {
            theFlag = flag.setIn(['data', 'id'], null);
          }
          itemsToSave.push(theFlag.get('data').toJS());
        } else {
          flags = flags.setIn([index, 'errors'], (0, _immutable.fromJS)(errors));
        }
      }
    });
    if (!errorsFound) {
      this.setState({
        isLoading: true
      });
      this.props.onSave(itemsToSave);
    } else {
      this.setState({
        flags
      });
    }
  };
  inDirtyState = () => this.state.flags.some(flag => flag.get('isDirty'));
  isDeactivated = flag => !flag.getIn(['data', 'isActive']);
  getItemTemplates = flag => flag.getIn(['data', 'itemTemplates'], new _immutable.List());
  sixHexColor = flag => {
    const color = flag.getIn(['data', 'color']);
    return color ? '#'.concat(color.substring(0, 6)).toUpperCase() : '#000000';
  };
  renderColor(item) {
    const id = item.getIn(['data', 'id']);
    return /*#__PURE__*/_react.default.createElement("div", {
      className: "color-column"
    }, /*#__PURE__*/_react.default.createElement(_2.SelectedColor, {
      color: this.sixHexColor(item),
      colors: _2.ColorPicker.FLAG_COLORS,
      isDisabled: this.isDeactivated(item),
      onChange: this.handleColorChange.bind(this, `${id}.data.color`)
    }));
  }
  renderDeactivate(item) {
    const deactivate = this.isDeactivated(item);
    const itemTemplates = this.getItemTemplates(item);
    const flagIsInUse = Boolean(itemTemplates.size);
    const disabled = flagIsInUse && !deactivate;
    const id = item.getIn(['data', 'id']);
    return /*#__PURE__*/_react.default.createElement("div", {
      className: "deactivate-column"
    }, /*#__PURE__*/_react.default.createElement(_2.Button, {
      color: _2.Button.LINK_COLORS.blue,
      disabled: disabled,
      onClick: deactivate ? this.activate.bind(this, id) : this.deactivate.bind(this, id),
      type: _2.Button.TYPES.link
    }, deactivate ? 'Activate' : 'Deactivate'), disabled && /*#__PURE__*/_react.default.createElement("p", {
      className: "disabled-text"
    }, "Used on ", itemTemplates.size, " list items."));
  }
  renderFlags() {
    const {
      flags
    } = this.state;
    const {
      hasNew,
      searchString,
      showAll
    } = this.state;
    return (0, _lodash.compact)(flags.filter(flag => {
      const id = flag.getIn(['data', 'id']);
      const name = flag.getIn(['data', 'name']) || '';
      if (hasNew && searchString === '' && !showAll) {
        return id.includes('temporary');
      }
      return name.toLocaleLowerCase().includes(searchString.toLocaleLowerCase()) || id.includes('temporary');
    }).map(flag => {
      const id = flag.getIn(['data', 'id']);
      return /*#__PURE__*/_react.default.createElement("div", {
        className: "table-row",
        key: id
      }, this.renderName(flag), this.renderColor(flag), this.renderSymbol(flag), this.renderDeactivate(flag));
    }).toJS());
  }
  renderName(item) {
    return /*#__PURE__*/_react.default.createElement("div", {
      className: "name-column"
    }, /*#__PURE__*/_react.default.createElement(_2.Field, {
      color: this.sixHexColor(item),
      errors: item.getIn(['errors', 'name'])?.toJS?.(),
      inputName: `${item.getIn(['data', 'id'])}.data.name`,
      isDisabled: this.isDeactivated(item),
      label: "Name",
      onChange: this.handleChange,
      validators: validators.name,
      value: item.getIn(['data', 'name'])
    }));
  }
  renderSymbol(item) {
    const id = item.getIn(['data', 'id']);
    return /*#__PURE__*/_react.default.createElement("div", {
      className: "symbol-column"
    }, /*#__PURE__*/_react.default.createElement(_2.Field, {
      color: this.sixHexColor(item),
      errors: item.getIn(['errors', 'symbol'])?.toJS?.(),
      inputName: `${id}.data.symbol`,
      isDisabled: this.isDeactivated(item),
      label: "Symbol",
      onChange: this.handleChange,
      type: _2.Field.TYPE_OPTIONS.emoji,
      value: item.getIn(['data', 'symbol'])
    }));
  }
  render() {
    const {
      isOpen,
      onClose,
      ownerName
    } = this.props;
    const {
      flags,
      hasNew,
      isLoading,
      searchString,
      showAll
    } = this.state;
    const rightButtons = [{
      text: 'Cancel',
      onClick: onClose
    }, {
      text: 'Save',
      onClick: this.handleSave,
      isLoading
    }];
    const subHeader = 'Flags used on list items cannot be deactivated until every reference to those flags have been removed.';
    const isDirty = this.inDirtyState();
    return /*#__PURE__*/_react.default.createElement(_FlagsDialog.default, {
      className: "flags-dialog"
    }, /*#__PURE__*/_react.default.createElement(_2.Dialog, {
      isOpen: isOpen,
      onClose: onClose
    }, /*#__PURE__*/_react.default.createElement(_2.Dialog.Header, {
      subtitle: subHeader,
      title: `${ownerName} Flags`
    }), /*#__PURE__*/_react.default.createElement(_2.Dialog.Content, null, /*#__PURE__*/_react.default.createElement(_2.Button, {
      color: _2.Button.LINK_COLORS.blue,
      onClick: this.addFlag,
      type: _2.Button.TYPES.link
    }, "Add Flag"), /*#__PURE__*/_react.default.createElement(_2.Field, {
      inputName: "flag-search",
      label: "Search",
      onChange: (_, value) => this.setState({
        searchString: value
      }),
      onTrailingIconClick: /* istanbul ignore next */
      () => this.setState({
        searchString: ''
      }),
      trailingIconName: "close-circle-outline",
      type: _2.Field.TYPE_OPTIONS.input,
      value: searchString
    }), (showAll || searchString !== '' || hasNew) && /*#__PURE__*/_react.default.createElement(_react.default.Fragment, null, /*#__PURE__*/_react.default.createElement("div", {
      className: "table-header"
    }, /*#__PURE__*/_react.default.createElement("div", {
      className: "name-column"
    }, "Name"), /*#__PURE__*/_react.default.createElement("div", {
      className: "color-column"
    }, "Text Color"), /*#__PURE__*/_react.default.createElement("div", {
      className: "symbol-column"
    }, "Symbol"), /*#__PURE__*/_react.default.createElement("div", {
      className: "deactivate-column"
    }, "Action")), /*#__PURE__*/_react.default.createElement("div", {
      className: "table-body"
    }, this.renderFlags())), !showAll && /*#__PURE__*/_react.default.createElement(_2.Button, {
      color: _2.Button.LINK_COLORS.blue,
      onClick: () => this.setState({
        showAll: true
      }),
      type: _2.Button.TYPES.link
    }, "Show All Flags (", flags.size, ")")), isDirty && /*#__PURE__*/_react.default.createElement("p", {
      className: "change-text body2"
    }, "Changes made here apply to all lists."), /*#__PURE__*/_react.default.createElement(_2.Dialog.ButtonFooter, {
      rightButtons: rightButtons
    })));
  }
}
FlagsDialog.propTypes = {
  flags: _reactImmutableProptypes.default.list,
  isOpen: _propTypes.default.bool,
  onClose: _propTypes.default.func,
  onSave: _propTypes.default.func,
  ownerName: _propTypes.default.string
};
FlagsDialog.defaultProps = {
  flags: (0, _immutable.fromJS)([]),
  isOpen: false,
  onClose: Function.prototype,
  onSave: Function.prototype,
  ownerName: 'Manage'
};
var _default = exports.default = FlagsDialog;