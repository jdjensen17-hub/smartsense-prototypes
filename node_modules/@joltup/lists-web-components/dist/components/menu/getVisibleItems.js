"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getVisibileItems;
var _lodash = require("lodash");
function getVisibileItems(groups) {
  if (!(0, _lodash.isArray)(groups)) {
    return [];
  }
  return (0, _lodash.compact)(groups.map(group => {
    const visibleItems = (0, _lodash.compact)(group.map(item => {
      const {
        isVisible
      } = item;
      const newItem = {
        ...item
      };
      delete newItem.isVisible;
      if (typeof isVisible === 'function') {
        return isVisible() ? newItem : null;
      } else if (typeof isVisible === 'boolean') {
        return isVisible ? newItem : null;
      }
      return newItem;
    }));
    return (0, _lodash.isEmpty)(visibleItems) ? null : visibleItems;
  }));
}