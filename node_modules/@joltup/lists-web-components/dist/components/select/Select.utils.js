"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.haveOptionsUpdated = void 0;
/**
 * haveOptionsUpdated - Determines whether Select options have been changed
 *
 * @param { Array<{value: String}> } previousOptions the previous array of options
 * @param { Array<{value: String}> } currentOptions the previous array of options
 *
 * @returns {boolean} Whether or not the Options are different
 */
const haveOptionsUpdated = (previousOptions, currentOptions) => {
  if (previousOptions && !currentOptions || !previousOptions && currentOptions) {
    return true;
  }
  if (previousOptions.length !== currentOptions.length) {
    return true;
  }
  for (const [index, item] of previousOptions.entries()) {
    if (item.value !== currentOptions[index].value) {
      return true;
    }
  }
  return false;
};
exports.haveOptionsUpdated = haveOptionsUpdated;