"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.convertRangeToMoment = convertRangeToMoment;
exports.listenToKeyDown = listenToKeyDown;
var _lodash = require("lodash");
var _momentTimezone = _interopRequireDefault(require("moment-timezone"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
/**
 * Converts an array of unix to an array of moment objects.
 *
 * @param { array } range - An array of two unix date values.
 * @param { string } timezone - The timezone to use for conversion
 *
 * @return { array } range - An array of two moment date values.
 */
function convertRangeToMoment(range, timezone = 'UTC') {
  return range.map(unix => {
    if ((0, _lodash.isNil)(unix)) {
      return null;
    }
    return (0, _momentTimezone.default)(_momentTimezone.default.unix(unix)).tz(timezone);
  });
}

/**
 * Handle key press events for left/right incrementing and decrementing the calendar.
 *
 * @param {object}.{function} decrementMonth - Callback to decrement the month.
 * @param {object}.{function} incrementMonth - Callback to increment the month.
 *
 * @return {function} callback to turn event listener off.
 */
function listenToKeyDown({
  decrementMonth,
  incrementMonth
}) {
  return () => {
    /**
     * Handle key press events.
     * Increment/decrement the calendar on left/right key press.
     *
     * @param {object} event - Keypress event.
     */
    function handleKeyDown(event) {
      if (event.keyCode === 37) {
        // Left arrow
        decrementMonth();
      } else if (event.keyCode === 39) {
        // Right arrow
        incrementMonth();
      }
    }
    window.addEventListener('keydown', handleKeyDown);
    return () => window.removeEventListener('keydown', handleKeyDown);
  };
}