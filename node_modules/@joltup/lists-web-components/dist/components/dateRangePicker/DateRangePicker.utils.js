"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.SINGLE_DAY_OPTIONS = exports.RANGE_OPTIONS = exports.DATE_DISPLAY_FORMAT = void 0;
exports.convertUnixRangeToString = convertUnixRangeToString;
exports.generateCallbackWhenNotDefaulted = generateCallbackWhenNotDefaulted;
exports.onInputFieldBlur = exports.onCalendarPickerChange = exports.onCalendarPickerBlur = exports.handleChangeOption = void 0;
exports.parsePossibleDateFormats = parsePossibleDateFormats;
exports.updateRange = exports.updateDisplayValue = void 0;
var _listsCore = require("@joltup/lists-core");
var _lodash = require("lodash");
var _momentTimezone = _interopRequireDefault(require("moment-timezone"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
const RANGE_OPTIONS = exports.RANGE_OPTIONS = Object.keys(_listsCore.CALENDAR_OPTIONS).reduce((result, key) => {
  // Don't show 'Date Range' as a dropdown option
  if (key !== _listsCore.CALENDAR_OPTION_DATE_RANGE) {
    result.push({
      value: key,
      label: _listsCore.CALENDAR_OPTIONS[key]
    });
  }
  return result;
}, []);
const SINGLE_DAY_OPTIONS = exports.SINGLE_DAY_OPTIONS = [{
  label: _listsCore.CALENDAR_OPTIONS[_listsCore.CALENDAR_OPTION_TODAY],
  value: _listsCore.CALENDAR_OPTION_TODAY
}, {
  label: _listsCore.CALENDAR_OPTIONS[_listsCore.CALENDAR_OPTION_YESTERDAY],
  value: _listsCore.CALENDAR_OPTION_YESTERDAY
}];
const DATE_DISPLAY_FORMAT = exports.DATE_DISPLAY_FORMAT = 'YYYY-MM-DD';

/**
 * Generate a callback function only if the calendar isOpen boolean has not been defaulted.
 *
 * @param {object} params - Parameters
 * @param {function} params.callback - The callback to be called.
 * @param {boolean} params.isCalendarOpen - Is the calendar defaulted to open.
 *
 * @returns {function} callback - The callback to be called.
 */
function generateCallbackWhenNotDefaulted({
  callback,
  isCalendarOpen
}) {
  if (isCalendarOpen === true) {
    return Function.prototype;
  }
  return () => callback();
}

/**
 * Converts range to unix timestamps to readable date strings
 *
 * @param {array} range - the currently selected date range ex. [1232312, 21312322]
 * @param {string} timezone - the timezone to use for conversion
 *
 * @returns {array} - An array of the start and end timestamps as readable strings ['1970-01-15', '1970-09-04']
 *
 * @example
 * convertUnixRangeToString([1232312, 21312322]) => ['1970-01-15', '1970-09-04']
 */
function convertUnixRangeToString(range, timezone = 'UTC') {
  return range.map(unix => {
    if ((0, _lodash.isNil)(unix)) {
      return null;
    }
    return (0, _momentTimezone.default)(_momentTimezone.default.unix(unix)).tz(timezone).format(DATE_DISPLAY_FORMAT);
  });
}

/**
 * Takes a date string and uses a few different masks to try and parse a moment date from the string
 *
 * @param {string} date - Date string to be parsed.
 * @param {string} timezone - The timezone to use
 *
 * @returns {moment} date - Moment date
 *
 * @example
 * parsePossibleDateFormats("7-4-19")
 */
function parsePossibleDateFormats(date, timezone = 'UTC') {
  const formatMasks = ['M-D-YY', 'MM-DD-YY', 'D-M-YY', 'M-D-YY', 'YYYY-MM-DD'];
  return _momentTimezone.default.tz(date, formatMasks, timezone);
}

/**
 * Calls the dispatch action to update the Visible text in the Start and End fields
 *
 * @param {function} changeDisplayValue - a dispatch function to update the field values
 *
 * @returns {function} callback - A callback function
 *
 * @example
 * const [state, dispatch] = useReducer(reducer, initialRange, init);
 *
 * updateDisplayValue(dispatch)('start', 2112312322)
 */
const updateDisplayValue = changeDisplayValue => (label, value) => {
  changeDisplayValue({
    type: label,
    payload: {
      value
    }
  });
};

/**
 * Calls the handleDateChange and updateField when the Start or End field is blurred
 *
 * @param {function} handleDateChange - a callback to send the user which field changed and the selected unix timestamp
 * @param {array} range - the currently selected date range ex. [1232312, 21312322]
 * @param {string} timezone - the timezone to use to parse the date into a timestamp
 *
 * @returns {function} callback - A callback function
 *
 * @example
 * onInputFieldBlur(handleDateChange, [ 2312312312, 3242424232], updateField)('start', 2112312322)
 */
exports.updateDisplayValue = updateDisplayValue;
const onInputFieldBlur = (handleDateChange, range, timezone = 'UTC') => (label, value) => {
  const parsedValue = parsePossibleDateFormats(value, timezone);
  if (parsedValue.isValid()) {
    let newValue = parsedValue.startOf('day').unix();
    if (label === 'end') {
      newValue = parsedValue.endOf('day').unix();
    }
    // set the new date value if it is a valid date
    handleDateChange({
      label,
      value: newValue,
      option: _listsCore.CALENDAR_OPTION_DATE_RANGE,
      changeOrigin: 'typed'
    });
  } else {
    // If the date is invalid, set state to the last valid one
    const unixValue = range[label === 'start' ? 0 : 1];
    handleDateChange({
      label,
      value: unixValue,
      option: _listsCore.CALENDAR_OPTION_DATE_RANGE,
      changeOrigin: 'typed'
    });
  }
};

/**
 * Calls the handleDateChange and updateField when the <CalendarPicker /> is blurred
 *
 * @param {function} handleDateChange - a callback to send the user which field changed and the selected unix timestamp
 *
 * @returns {function} callback - A callback function
 *
 * @example
 * onCalendarPickerChange(handleDateChange, updateField)('start', 2112312322)
 */
exports.onInputFieldBlur = onInputFieldBlur;
const onCalendarPickerChange = handleDateChange => (label, value) => {
  handleDateChange({
    label,
    value,
    option: _listsCore.CALENDAR_OPTION_DATE_RANGE,
    changeOrigin: 'calendar'
  });
};

/**
 * onCalendarPickerBlur - Calls the handleDateChange and updateField when the <CalendarPicker /> is blurred
 *
 * @param {function} handleDateChange - a callback to send the user which field changed and the selected unix timestamp
 * @param {function} setClosed - a callback to close the calendar picker
 * @param {array} range - the currently selected date range ex. [1232312, 21312322]
 * @param {string} timezone - the timezone to use for conversion
 *
 * @returns {function} callback - A callback function
 */
exports.onCalendarPickerChange = onCalendarPickerChange;
const onCalendarPickerBlur = (handleDateChange, setClosed, range, timezone = 'UTC') => () => {
  if ((0, _lodash.isNil)(range[1])) {
    handleDateChange({
      label: 'end',
      value: (0, _momentTimezone.default)(_momentTimezone.default.unix(range[0])).tz(timezone).endOf('day').unix(),
      option: _listsCore.CALENDAR_OPTION_DATE_RANGE,
      changeOrigin: 'calendar'
    });
  }
  setClosed();
};

/**
 * Calls the handleDateFieldChange and updateField when a new option is selected in the dropdown
 *
 * @param {function} handleDateChange - a callback to send the user which field changed and the selected unix timestamp
 * @param {array} range - the currently selected date range ex. [1232312, 21312322]
 * @param {string} timezone - the timezone to use when determining the date range
 *
 * @returns {function} callback - A callback function
 *
 * @example
 * handleChangeOption(handleDateChange, [2312312312, 3242424232], updateField)('option', 'PAST_WEEK')
 */
exports.onCalendarPickerBlur = onCalendarPickerBlur;
const handleChangeOption = (handleDateChange, range, timezone = 'UTC') => (label, value) => {
  let updatedRange = range;
  if (value !== _listsCore.CALENDAR_OPTION_DATE_RANGE) {
    const [startDate, endDate] = (0, _listsCore.getDateRangeFromFilter)({
      option: value
    }, timezone);
    updatedRange = [startDate, endDate];
  }
  handleDateChange({
    label,
    value: updatedRange,
    option: value,
    changeOrigin: 'option'
  });
};

/**
 * Called to update the Start and End values when the range changes
 *
 * @param {array} range - [ timestamp, timestamp ] - array with the start and end range unix values
 * @param {function} updateDisplayedValues - The function called to update the Start and End displayed values
 * @param {string} timezone - The timezone to use when updating the values
 *
 * @returns {function} callback - Callback function
 */
exports.handleChangeOption = handleChangeOption;
const updateRange = (range, updateDisplayedValues, timezone) => () => {
  const stringRange = convertUnixRangeToString(range, timezone);
  updateDisplayedValues({
    type: 'range',
    payload: {
      value: {
        start: stringRange[0],
        end: stringRange[1]
      }
    }
  });
};
exports.updateRange = updateRange;