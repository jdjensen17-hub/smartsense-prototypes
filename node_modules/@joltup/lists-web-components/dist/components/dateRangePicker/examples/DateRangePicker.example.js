"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.DateRangePickerExample = DateRangePickerExample;
var _react = _interopRequireWildcard(require("react"));
var _moment = _interopRequireDefault(require("moment"));
var _listsCore = require("@joltup/lists-core");
var _dateRangePicker = _interopRequireDefault(require("../../dateRangePicker"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _interopRequireWildcard(e, t) { if ("function" == typeof WeakMap) var r = new WeakMap(), n = new WeakMap(); return (_interopRequireWildcard = function (e, t) { if (!t && e && e.__esModule) return e; var o, i, f = { __proto__: null, default: e }; if (null === e || "object" != typeof e && "function" != typeof e) return f; if (o = t ? n : r) { if (o.has(e)) return o.get(e); o.set(e, f); } for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]); return f; })(e, t); }
function DateRangePickerExample() {
  function init(initialRange) {
    return {
      range: initialRange
    };
  }
  function reducer(state, action) {
    switch (action.type) {
      default:
      case 'setStartRange':
        return {
          range: [action.payload.value, null]
        };
      case 'setStartField':
        return {
          range: [action.payload.value, state.range[1]]
        };
      case 'setEnd':
        return {
          range: [state.range[0], action.payload.value]
        };
      case 'setRange':
        return {
          range: action.payload.value
        };
    }
  }
  const now = (0, _moment.default)();
  const nowUnix = now.unix();
  const lastWeek = now.clone().subtract(7, 'days').unix();
  const initialRange = [lastWeek, nowUnix];
  const [state, dispatch] = (0, _react.useReducer)(reducer, initialRange, init);
  const [option, setOption] = (0, _react.useState)(null);
  const handleDateChange = props => {
    setOption(props.option);
    if (props.option === _listsCore.CALENDAR_OPTION_DATE_RANGE) {
      if (props.label === 'start' && props.changeOrigin === 'typed') {
        dispatch({
          type: 'setStartField',
          payload: {
            value: props.value
          }
        });
      } else if (props.label === 'start' && props.changeOrigin === 'calendar') {
        dispatch({
          type: 'setStartRange',
          payload: {
            value: props.value
          }
        });
      } else if (props.label === 'end') {
        dispatch({
          type: 'setEnd',
          payload: {
            value: props.value
          }
        });
      }
    } else {
      dispatch({
        type: 'setRange',
        payload: {
          value: props.value
        }
      });
    }
  };
  return /*#__PURE__*/_react.default.createElement(_dateRangePicker.default, {
    handleDateChange: handleDateChange,
    option: option,
    range: state.range
  });
}