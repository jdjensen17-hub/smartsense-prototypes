"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.OPTION_TYPES = void 0;
exports.filterSelectedFromOptions = filterSelectedFromOptions;
exports.generateKeyboardPressedCallback = void 0;
exports.generateOnAddCallback = generateOnAddCallback;
exports.getThreshold = void 0;
exports.keyboardAdjustScrollLocation = keyboardAdjustScrollLocation;
var _lodash = require("lodash");
const OPTION_TYPES = exports.OPTION_TYPES = Object.freeze({
  HEADER: 'header',
  OPTION: 'option'
});

/**
 * Generate an onAdd callback function
 *
 * @params {object}.{string} id - The option's unique id.
 * @params {object}.{string} label - The label of the option.
 * @params {object}.{function} onAdd - The onAdd callback to be called within this callback.
 *
 * @returns {function} handleAdd - The handleAdd callback function.
 */
function generateOnAddCallback({
  onAdd,
  option
}) {
  return () => (0, _lodash.isNil)(option) ? {} : onAdd({
    id: option.id,
    label: option.label
  });
}

/**
 * Removes selected values from the options list
 *
 * @param {array} selected - List of selected values
 * @param {array} options - List of all possible values
 *
 * @returns {array} options - Filtered options
 */
function filterSelectedFromOptions(selected = [], options = []) {
  return options.filter(option => !selected.find(selection => selection.id === option.id));
}

/**
 * Scrolls list to the keyboard selected option
 *
 * @param {object}.{object} activeOptionRef - Selected option DOM element
 * @param {object}.{object} optionsRef - Options list DOM element
 * @param {object}.{function} setScrollLocation - Callback function to set scroll location
 */
function keyboardAdjustScrollLocation({
  activeOptionRef,
  optionsRef,
  setScrollLocation
}) {
  if (activeOptionRef) {
    if (activeOptionRef.offsetTop < optionsRef.scrollTop) {
      // Align scroll location to selected option at top
      setScrollLocation(activeOptionRef.offsetTop);
    } else if (activeOptionRef.offsetTop >= optionsRef.scrollTop + optionsRef.clientHeight) {
      // Align scroll location to selected option at bottom
      const adjustOffset = optionsRef.clientHeight - activeOptionRef.clientHeight;
      const newPosition = activeOptionRef.offsetTop - adjustOffset;
      setScrollLocation(newPosition);
    }
  }
}

/**
 * Generate a keyboard pressed callback
 *
 * @param {object}.{function} addOption - Callback when adding an option from the list
 * @param {object}.{string} code - Event code (example values: 'ArrowDown', 'Enter')
 * @param {object}.{number} highlightedOptionIndex - Current keyboard selected option
 * @param {object}.{object} unsortedOptions - List of unsorted options
 * @param {object}.{function} setHighlightedOptionIndex - Callback to set currently selected option
 * @param {object}.{bool} shouldSeparate - If should separate group options
 *
 * @returns {function} callback - Callback function
 */
const generateKeyboardPressedCallback = ({
  addOption,
  code,
  highlightedOption,
  unsortedOptions,
  setHighlightedOption,
  shouldSeparate
}) => {
  const options = shouldSeparate ? [...unsortedOptions].sort((a, b) => b.isActive - a.isActive) // put active ones first if grouping
  : unsortedOptions;
  const optionItemIndex = options.findIndex(({
    id
  }) => highlightedOption.id === id);
  const optionItem = options.find(({
    id
  }) => highlightedOption.id === id);
  switch (code) {
    case 'ArrowDown':
      if (optionItemIndex + 1 < options.length) {
        return () => setHighlightedOption(options[optionItemIndex + 1]);
      }
      break;
    case 'ArrowUp':
      if (optionItemIndex - 1 > -2 && !(0, _lodash.isNil)(options[optionItemIndex - 1])) {
        return () => setHighlightedOption(options[optionItemIndex - 1]);
      }
      break;
    case 'Enter':
      if (optionItemIndex - 1 > -2 && !(0, _lodash.isNil)(options[optionItemIndex - 1])) {
        setHighlightedOption(options[optionItemIndex - 1]);
      }
      addOption(optionItem)();
      break;
    default:
      return () => setHighlightedOption({});
  }
  return () => {};
};

/**
 * Get the threshold when to load more results. If there are inactive options
 * we want to get the height of the inactive item div so the loading begins when the end
 * of the active options has been reached.
 *
 * @param {Object} inactiveOptionsRef Inactive options div object to get height from
 *
 * @returns {Number} Threshold
 */
exports.generateKeyboardPressedCallback = generateKeyboardPressedCallback;
const getThreshold = inactiveOptionsRef => Math.max((0, _lodash.get)(inactiveOptionsRef, 'current.clientHeight', 0) + 1000, 1000);
exports.getThreshold = getThreshold;