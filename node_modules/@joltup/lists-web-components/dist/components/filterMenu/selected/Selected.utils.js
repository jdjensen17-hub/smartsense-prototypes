"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.generateHandleRemoveCallback = generateHandleRemoveCallback;
/**
 * Generate a remove callback function.
 *
 * @param {object}.{string} id - The unique id of the listTemplate to remove.
 * @param {object}.{function} onRemove - The remove callback to be called within this callback.
 *
 * @return {function} handleRemove - The handle remove callback.
 */
function generateHandleRemoveCallback({
  id,
  onRemove
}) {
  return () => onRemove(id);
}