"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getMaxDimensions = getMaxDimensions;
/**
 * Get the signature dimensions.
 *
 * @param {array} points - Array of points for a signature
 * @param {object} points.point - A single line of a signature
 * @param {integer} points.point.mx - The x location of the first point.
 * @param {integer} points.point.my - The y location of the first point.
 * @param {integer} points.point.lx - The x location of the first point.
 * @param {integer} points.point.ly - The y location of the first point.
 *
 * @return {object} values - An object of max x and y.
 * @return {integer} values.maxX - Maximum X value of signature.
 * @return {integer} values.maxY - Maximum Y value of signature.
 */
function getMaxDimensions(points) {
  let maxX = 0;
  let maxY = 0;
  points?.forEach(point => {
    if (point.mx > maxX) {
      maxX = point.mx;
    }
    if (point.lx > maxX) {
      maxX = point.lx;
    }
    if (point.my > maxY) {
      maxY = point.my;
    }
    if (point.ly > maxY) {
      maxY = point.ly;
    }
  });
  return {
    maxX,
    maxY
  };
}