"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.defaultDataJSON = void 0;
exports.getInitialFormDemoSelectedTemplate = getInitialFormDemoSelectedTemplate;
exports.sortOrderCompare = sortOrderCompare;
var _lodash = require("lodash");
/** Template/demo rows carry `sortOrder` for ordering; it is not part of `OptionSetDataItem`. */

/**
 * Storybook / demos: show choices on first paint without selecting a template or adding rows.
 * - Create mode: empty template id with item rows cloned from the first catalog entry.
 * - Template mode: first catalog template selected.
 */
function getInitialFormDemoSelectedTemplate(catalog, isTemplate) {
  const first = catalog[0];
  if (isTemplate) {
    return (0, _lodash.clone)(first);
  }
  return {
    id: '',
    name: '',
    itemOptions: (0, _lodash.clone)(first.itemOptions)
  };
}

/** Shared helpers for OptionSet Storybook examples (avoids importing from `*.stories`). */
function sortOrderCompare(a, b) {
  return (a.sortOrder ?? 0) - (b.sortOrder ?? 0);
}
const defaultDataJSON = exports.defaultDataJSON = `[
  { "id": "abc", "value": "Some Text", "sortOrder": 2 },
  { "id": "def", "value": "Another Text", "sortOrder": 1 },
  { "id": "ghi", "value": "One More Text", "sortOrder": 3 }\n]`;