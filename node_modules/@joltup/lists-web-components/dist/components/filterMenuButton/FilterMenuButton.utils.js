"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getShouldRightAlignMenu = exports.FILTER_MENU_PIXEL_WIDTH = void 0;
var _lodash = require("lodash");
const FILTER_MENU_PIXEL_WIDTH = exports.FILTER_MENU_PIXEL_WIDTH = 424;

/**
 * Determines if the popup menu of a filter should be right aligned. This will be true if there is
 * not enough room to left align the menu, but there is enough room to right align it.
 * @param {number} distanceToLeft number of pixels to left side of screen from button
 * @param {number} menuWidth width of popup menu
 * @returns {boolean} whether the popup menu should right align
 */
const getShouldRightAlignMenu = (distanceToLeft, menuWidth) => {
  if ((0, _lodash.isNil)(distanceToLeft) || (0, _lodash.isNil)(menuWidth)) {
    return false;
  }
  const tooBigForLeftAlign = distanceToLeft + menuWidth > window.innerWidth;
  const tooBigForRightAlign = distanceToLeft < menuWidth;
  return tooBigForLeftAlign && !tooBigForRightAlign;
};
exports.getShouldRightAlignMenu = getShouldRightAlignMenu;