"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onOpen = exports.onCancel = exports.PLACEMENTS = void 0;
const PLACEMENTS = exports.PLACEMENTS = ['bottom',
// 0
'bottom-start',
// 1
'bottom-end',
// 2
'left',
// 3
'left-start',
// 4
'left-end',
// 5
'right',
// 6
'right-start',
// 7
'right-end',
// 8
'top',
// 9
'top-start',
// 10
'top-end' // 11
];

/**
 * The function that is called to make the tooltip visible
 *
 * @param {boolean} isOpening - If tooltip is already open or timer is running to open tooltip
 * @param {function} setOpening - The function called that starts the tooltip open timer
 *
 * @returns {function} callback - A callback function.
 */
const onOpen = (isOpening, setOpening) => () => {
  if (!isOpening) {
    setOpening(true);
  }
};

/**
 * The function that is called to cancel making the tooltip appear
 *
 * @param {boolean} isOpening - If tooltip is open or timer running to open tooltip
 * @param {function} setOpening - the function called that cancels opening the tooltip
 *
 * @returns {function} callback - A callback function.
 */
exports.onOpen = onOpen;
const onCancel = (isOpening, setOpening) => () => {
  if (isOpening) {
    setOpening(false);
  }
};
exports.onCancel = onCancel;