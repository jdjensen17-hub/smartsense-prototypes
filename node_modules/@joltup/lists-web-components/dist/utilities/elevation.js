"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.elevation = elevation;
exports.reverseElevation = reverseElevation;
var _colors = _interopRequireDefault(require("@joltup/colors"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
/**
 * @typedef {Object} Elevation
 * @property {String} boxShadow - css string
 */

/**
 * Creates an object of CSS properties to create the Material elevation effect
 * @param {int} amount The amount of elevation to be added
 * @returns {Elevation} Object with elevation css properties
 */
function elevation(amount) {
  return {
    boxShadow: `0px ${amount + 1}px ${amount + 1}px ${_colors.default.black.disabled}`
  };
}

/**
 * Creates an object of CSS properties to create the Material elevation effect for the reverse side
 * @param {int} amount The amount of elevation to be added
 * @returns {Elevation} Object with elevation css properties
 */
function reverseElevation(amount) {
  return {
    boxShadow: `0px -${amount / 2 + 1}px ${amount + 1}px ${_colors.default.black.disabled}`
  };
}