"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ripple = ripple;
/**
 * @typedef {Object} Ripple
 * @property {String} backgroundPosition - css string
 * @property {String} transition - css string
 * @property {String} background - css string
 * @property {String} backgroundColor - css string
 * @property {String} backgroundSize - css string
 * @property {String} transition - css string
 */
/**
 * @typedef {Object} RippleParams
 * @property {Object} [activeStyles] - style object
 * @property {String} [backgroundColor] - css string
 * @property {Object} [hoverStyles] - style object
 * @property {Number} [transitionSpeed] - amount of miliseconds
 * @property {String} [rippleColor] - css string
 * @property {String} [selector] - item to select
 */

/**
 * Creates an object of CSS properties to create Ripple effect
 * @param {RippleParams} RippleParams The parameters to create the ripple CSS
 * @returns {Ripple} Object with ripple css properties
 */
function ripple({
  activeStyles = {},
  backgroundColor,
  hoverStyles = {},
  transitionSpeed = 350,
  rippleColor,
  selector // apply :hover and :active styles to a nested selector from the current selector
}) {
  const hoverSelector = selector ? `&:hover ${selector}` : '&:hover';
  const activeSelector = selector ? `&:active ${selector}` : '&:active';
  return {
    backgroundPosition: 'center',
    transition: `background ${transitionSpeed}ms`,
    [hoverSelector]: {
      ...hoverStyles,
      background: `${backgroundColor} radial-gradient(circle, transparent 1%, ${backgroundColor} 1%) center/15000%`
    },
    [activeSelector]: {
      ...activeStyles,
      backgroundColor: rippleColor,
      backgroundSize: '100%',
      transition: 'background 0s'
    }
  };
}