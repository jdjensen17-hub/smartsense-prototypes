"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getZIndex = getZIndex;
exports.zIndexOptions = void 0;
const zIndexOptions = exports.zIndexOptions = {
  dialog: 'Dialog',
  fab: 'Fab',
  header: 'Header',
  menu: 'Menu'
};

/**
 * zIndexesOrder defines the list of elements as they appear on top of each other.
 */
const zIndexOrder = [zIndexOptions.header, zIndexOptions.fab, zIndexOptions.menu, zIndexOptions.dialog];

/**
 * This function is used to enforce order of elements.
 * zIndexesOrder defines the list of elements as they appear on top of each other.
 * The first element in zIndexesOrder would have z-index of 125.
 * The second element in zIndexesOrder would have z-index of 150.
 * Each product team could then define elements that live between these elements.
 *
 * @param {string} key - Unique key identifying a type of element.
 *
 * @return {number} zIndex - The keys z-index on the page.
 *
 * @example
 *
 * z-index: getZIndex('dialog') + 1;
 *
 * The line above would place an element right above the dialog component.
 */
function getZIndex(key) {
  return (zIndexOrder.indexOf(key) + 1) * 25 + 100;
}