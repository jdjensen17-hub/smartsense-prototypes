"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.apolloClient = void 0;
exports.getGraphQLApiUrl = getGraphQLApiUrl;
exports.graphQLApiPath = void 0;
var _client = require("@apollo/client");
var _cache = require("@apollo/client/cache");
var _error = require("@apollo/client/link/error");
var _listsCore = require("@joltup/lists-core");
var _crossFetch = _interopRequireDefault(require("cross-fetch"));
var _clam = _interopRequireDefault(require("@joltup/shared-dev-utils/utils/clam"));
var _getCompanyId = require("@joltup/shared-dev-utils/utils/getCompanyId");
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
const graphQLApiPath = exports.graphQLApiPath = '/graphql';

/**
 * Returns the appropriate url to the graphQL api.
 *
 * @return {string} url - The appropriate url to the graphQL api.
 */
function getGraphQLApiUrl() {
  if (window.location.hostname === 'localhost') {
    return `http://localhost:30041${graphQLApiPath}`;
  }

  // For use in the Gitlab CI pipeline
  if (window.location.hostname === 'docker') {
    return `http://docker:30041${graphQLApiPath}`;
  }
  const hostArray = window.location.hostname.split('.');
  const domainName = hostArray[1];
  return `https://api.${domainName}.com${graphQLApiPath}`;
}
const cache = new _cache.InMemoryCache();
const defaultLink = {
  credentials: 'include',
  fetch: _crossFetch.default,
  headers: {
    jolt_companyid: (0, _getCompanyId.getCompanyId)()
  },
  uri: getGraphQLApiUrl()
};

// This code defines an Apollo Link that will be used to make HTTP requests to a GraphQL API.
// The `split` function takes a test function, and two links, and returns a new link.
// If the test function returns `true` for an operation, the first link will be used,
// otherwise the second link will be used.
// This allows different headers to be used for specific operations, or for different contexts.
const httpLink = _client.ApolloLink.split(operation => operation.getContext().noDefaultHeaders === true, new _client.HttpLink({
  ...defaultLink,
  headers: {}
}), new _client.HttpLink({
  ...defaultLink
}));

// This code defines an Apollo Link that will be used to handle errors that occur
// during GraphQL operations. The `onError` function takes a callback that will be
// called whenever an error occurs. The callback receives an object that contains
// information about any GraphQL or network errors that occurred.
const onErrorLink = new _error.ErrorLink(({
  operation
}) => {
  const {
    graphQLErrors,
    networkError
  } = operation.getContext();
  if (graphQLErrors) {
    _clam.default.log(_clam.default.SECTIONS.ENVIRONMENT, 'GraphQL Error', _clam.default.ACTIONS.ERROR, 'Apollo', JSON.stringify(graphQLErrors));
    console.error(`[Jolt GraphQL Error]: ${JSON.stringify(graphQLErrors)}`);
    if (graphQLErrors.includes('["token invalid"]') || graphQLErrors.includes('["jwt expired"]')) {
      (0, _listsCore.refreshAuthToken)();
    }
  }
  if (networkError) {
    _clam.default.log(_clam.default.SECTIONS.ENVIRONMENT, 'Network Error', _clam.default.ACTIONS.ERROR, 'Apollo', JSON.stringify(networkError));
    console.error(`[Network error]: ${JSON.stringify(networkError)}`);
  }
});
const apolloClient = exports.apolloClient = new _client.ApolloClient({
  cache,
  link: _client.ApolloLink.from([onErrorLink, httpLink])
});