import type { TemplateNotification, TemplateNotificationUpdate } from '../../types';
import { JoltRecordWrapper } from '../index';
export declare class TemplateNotificationWrapper extends JoltRecordWrapper<TemplateNotificationUpdate> {
    private _results;
    private _roles;
    private _people;
    private _property;
    private _methods;
    private _threshold;
    private _type;
    readonly contentGroup: TemplateNotification['contentGroup'];
    readonly location: TemplateNotification['location'];
    readonly template: TemplateNotification['template'];
    constructor({ contentGroup, location, roles, people, property, methods, type, results, template, threshold, update, ...joltRecord }: TemplateNotification);
    /**
     * Returns the results of the template notification with precedence to the update.
     * @returns {NotificationResult[]} The results of the template notification.
     */
    get results(): TemplateNotification['results'];
    /**
     * Returns the roles of the template notification with precedence to the update.
     * @returns {Role[]} The roles of the template notification.
     */
    get roles(): TemplateNotification['roles'];
    /**
     * Returns the people of the template notification with precedence to the update.
     * @returns {Person[]} The people of the template notification.
     */
    get people(): TemplateNotification['people'];
    /**
     * Returns the template property of the template notification with precedence to the update.
     * @returns {Pick<Item, 'parentId'>} The template property of the template notification.
     */
    get property(): TemplateNotification['property'];
    /**
     * Returns the notification methods of the template notification with precedence to the update.
     * @returns {NotificationMethod} The notification methods of the template notification.
     */
    get methods(): TemplateNotification['methods'];
    /**
     * Returns the threshold of the template notification with precedence to the update.
     * @returns {number} The threshold of the template notification.
     */
    get threshold(): TemplateNotification['threshold'];
    /**
     * Returns the notification type of the template notification with precedence to the update.
     * @returns {TemplateNotificationType} The notification type of the template notification.
     */
    get type(): TemplateNotification['type'];
    applyUpdates(): void;
    createRecordCopy(): TemplateNotification;
    toCreatePropertyInputObject(): {
        methods: import("../../types").NotificationMethod[];
        propertyParentId: string;
        results: (string | 0 | 1 | null)[];
        roleIds: string[];
        templateId: string;
        type: import("../../types").TemplateNotificationType;
    };
    toCreateTemplateInputObject(): {
        type: import("../../types").TemplateNotificationType;
        threshold?: number | undefined;
        methods: import("../../types").NotificationMethod[];
        roleIds: string[];
        templateId: string;
    };
    toUpdatePropertyInputObject(): {
        type?: import("../../types").TemplateNotificationType | undefined;
        id: string;
        methods: import("../../types").NotificationMethod[];
        results: (string | 0 | 1 | null)[];
        roleIds: string[];
    };
    toUpdateTemplateInputObject(): {
        type?: import("../../types").TemplateNotificationType | undefined;
        id: string;
        methods: import("../../types").NotificationMethod[];
        roleIds: string[];
    };
}
