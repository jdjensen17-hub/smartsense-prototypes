import { JoltRecordWrapper } from '../JoltRecordWrapper';
import type { CreateTagInput, UpdateTagInput } from '../../types';
import type { Tag, TagUpdate } from '../../types/tag';
type ValidationError = {
    field: string;
    messages: string[];
};
export declare class TagWrapper extends JoltRecordWrapper<TagUpdate> {
    private _color;
    private _icon;
    private _name;
    readonly contentGroup?: Tag['contentGroup'];
    readonly location?: Tag['location'];
    readonly types: Tag['types'];
    constructor(tag: Tag);
    get color(): Tag['color'] | null;
    get icon(): Tag['icon'] | null;
    get name(): Tag['name'];
    get validationErrors(): ValidationError[];
    applyUpdate(): void;
    toInputObject(): CreateTagInput | UpdateTagInput;
}
export {};
