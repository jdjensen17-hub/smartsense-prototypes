"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ItemResultWrapper = void 0;
var _types = require("../../types");
var _JoltRecordWrapper = require("../JoltRecordWrapper");
var _ItemWrapper = require("../ItemWrapper");
var _ItemResultWrapper = require("./ItemResultWrapper.utils");
class ItemResultWrapper extends _JoltRecordWrapper.JoltRecordWrapper {
  static recordTypesMap = {
    asset: 'AssetInstanceProperty',
    auditList: 'ItemResult',
    list: 'ItemResult',
    workOrder: 'WorkOrderInstanceProperty'
  };
  constructor(itemResult, recordType) {
    super(itemResult);
    const valueKey = (0, _ItemResultWrapper.getItemResultValueKey)(itemResult, recordType);
    this._valueKey = valueKey;
    // If the valueKey is null, then the item can't have a value
    if (valueKey) {
      const value = itemResult[valueKey];
      if (value === undefined || value === null) {
        this._value = _ItemResultWrapper.emptyValueMap.get(itemResult.type) ?? null;
      } else {
        this._value = value;
      }
    } else {
      this._value = null;
    }
    this._completionTimestamp = itemResult.completionTimestamp;
    this.calculatedScore = itemResult.calculatedScore;
    this.maxPossibleScore = itemResult.maxPossibleScore;
    this.recordType = recordType;
    this.siteAttribute = itemResult.siteAttribute;
    this.type = itemResult.type;
    const classItemTemplate = itemResult.itemTemplate ?? itemResult.templateProperty;
    this.itemTemplateWrapper = classItemTemplate ? new _ItemWrapper.ItemWrapper(classItemTemplate, _ItemResultWrapper.itemRecordTypeMap[recordType] ?? 'Item') : null;
    const classListInstance = itemResult.listInstance ?? itemResult.instance;
    this.listInstance = classListInstance;
  }
  get completionTimestamp() {
    return this._update?.completionTimestamp ?? this._completionTimestamp ?? null;
  }

  /**
   * Returns a string representation of the item result's value.
   * Currently used for the Leave Behind Report displaying item results.
   * PHOTO and SIGNATURE types can't be usefully displayed as a string.
   * @returns {string} The display value of the item result.
   */
  get displayValue() {
    if (this.value === null) {
      return null;
    }
    switch (this.type) {
      case _types.ItemType.ASSETS:
        {
          const value = this.value;
          return value.map(instance => instance.name).join(', ');
        }
      case _types.ItemType.CHECKMARK:
        return this.value === 1 ? 'Checked' : 'Unchecked';
      case _types.ItemType.DATE:
        {
          const value = this.value;
          return new Date(value * 1000).toLocaleDateString();
        }
      case _types.ItemType.DATE_TIME:
        {
          const value = this.value;
          return new Date(value * 1000).toLocaleString();
        }
      case _types.ItemType.EMAIL:
        {
          const value = this.value;
          return value.join(', ');
        }
      case _types.ItemType.FREE_RESPONSE:
        {
          return this.value;
        }
      case _types.ItemType.LOCATION:
        {
          const value = this.value;
          return value.name;
        }
      case _types.ItemType.LOCATION_TAGS:
        {
          const value = this.value;
          return value.map(tag => tag.name).join(', ');
        }
      case _types.ItemType.MULTIPLE_CHOICE:
        {
          const value = this.value;
          if (Array.isArray(value)) {
            return value.map(option => option.value).join(', ');
          }
          return value.value;
        }
      case _types.ItemType.NUMBER:
        {
          return this.value.toString();
        }
      case _types.ItemType.PERSON:
        {
          const value = this.value;
          return `${value.firstName} ${value.lastName}`;
        }
      case _types.ItemType.PHOTO:
        {
          return 'Item answered with photo';
        }
      case _types.ItemType.SHORT_ENTRY:
        {
          return this.value;
        }
      case _types.ItemType.SIGNATURE:
        {
          return 'Item answered with signature';
        }
      case _types.ItemType.STOPWATCH:
        {
          if (this.isActiveStopwatch) {
            return null;
          }
          return this.value.toString();
        }
      case _types.ItemType.STORE_ID:
        {
          return this.value;
        }
      case _types.ItemType.SUB_LIST:
        {
          return null;
        }
      case _types.ItemType.SUBTITLE:
        {
          return this.value;
        }
      case _types.ItemType.TEXT:
        {
          return this.value;
        }
      case _types.ItemType.YES_NO:
        {
          return this.value === 1 ? 'Yes' : 'No';
        }
      default:
        {
          // This will cause a compilation error if there are unhandled item types
          const catcher = this.type;
          throw new Error(`ItemResult type not supported: ${catcher}`);
        }
    }
  }

  /**
   * Returns if the item is a stopwatch that is active.
   * @returns {boolean} If the stopwatch is active.
   */
  get isActiveStopwatch() {
    return this.type === _types.ItemType.STOPWATCH && this.completionTimestamp === null && this.value !== null;
  }

  /**
   * Returns the "root" value of the item result based on its type.
   * If this is a string or number it will return the value directly, otherwise it will be the id if its an object or an array of ids if the result is plural.
   * For example, PERSON type will return the person's id
   * @returns {ItemResultRootValue} The value of the item result.
   */
  get rootValue() {
    switch (this.type) {
      case _types.ItemType.ASSETS:
        {
          if (this.value === null) {
            return null;
          }
          const value = this.value;
          return value.map(instance => instance.id);
        }
      case _types.ItemType.CHECKMARK:
      case _types.ItemType.DATE:
      case _types.ItemType.DATE_TIME:
      case _types.ItemType.NUMBER:
      case _types.ItemType.STOPWATCH:
      case _types.ItemType.YES_NO:
        return this.value;
      case _types.ItemType.FREE_RESPONSE:
      case _types.ItemType.SHORT_ENTRY:
      case _types.ItemType.SIGNATURE:
      case _types.ItemType.STORE_ID:
      case _types.ItemType.SUBTITLE:
        return this.value;
      case _types.ItemType.EMAIL:
        return this.value;
      case _types.ItemType.LOCATION:
        {
          if (this.value === null) {
            return null;
          }
          const value = this.value;
          return value?.id;
        }
      case _types.ItemType.LOCATION_TAGS:
        {
          if (this.value === null) {
            return null;
          }
          const value = this.value;
          return value.map(tag => tag.id);
        }
      case _types.ItemType.MULTIPLE_CHOICE:
        {
          if (this.value === null) {
            return null;
          }
          const value = this.value;
          return value.map(option => option.id);
        }
      case _types.ItemType.PERSON:
        {
          if (this.value === null) {
            return null;
          }
          const value = this.value;
          return value?.id;
        }
      case _types.ItemType.PHOTO:
        {
          if (this.value === null) {
            return null;
          }
          const value = this.value;
          return value.map(file => {
            return file.id;
          });
        }
      case _types.ItemType.SUB_LIST:
        {
          return null;
        }
      case _types.ItemType.TEXT:
        {
          return this.value;
        }
      default:
        {
          // This will cause a compilation error if there are unhandled item types
          const catcher = this.type;
          throw new Error(`ItemResult type not supported: ${catcher}`);
        }
    }
  }

  /**
   * Returns the value of the item result based on its type.
   * @returns {Value} The value of the item result.
   */
  get value() {
    return this._update?.value !== undefined ? this._update.value : this._value;
  }
  get valueKey() {
    return this._valueKey;
  }

  /**
   * Updates the item result with the provided update.
   * @param {ItemResultUpdate} update The update to apply.
   * @returns {void}
   */
  update(update) {
    try {
      (0, _ItemResultWrapper.verifyItemResultValue)(update.value, this.type);
    } catch (error) {
      console.error(error);
      return;
    }
    super.update({
      value: update.value,
      ...(this.type === _types.ItemType.STOPWATCH && this.completionTimestamp === null && {
        completionTimestamp: Math.floor(Date.now() / 1000)
      })
    });
  }
  toInputObject(mode) {
    switch (this.recordType) {
      case 'ItemResult':
        {
          throw new Error('ItemResult recordType not supported');
        }
      case 'AssetInstanceProperty':
      case 'WorkOrderInstanceProperty':
        {
          if (this.isNew && mode !== 'update') {
            if (!this.listInstance) {
              throw new Error('InstanceProperty must have a parent instance to create');
            }
            if (!this.itemTemplateWrapper) {
              throw new Error('InstanceProperty must have a parent template to create');
            }
            return {
              instanceId: this.listInstance.id,
              parentTemplatePropertyId: this.itemTemplateWrapper.parentId,
              property: this.rootValue
            };
          }
          return {
            auditComment: `Changed to ${this.rootValue}`,
            id: this.id,
            property: this.rootValue,
            ...(this.type === _types.ItemType.STOPWATCH && {
              completionTimestamp: this.completionTimestamp
            })
          };
        }
      default:
        {
          // This will cause a compilation error if there are unhandled record types
          const catcher = this.recordType;
          throw new Error(`ItemResult recordType not supported: ${catcher}`);
        }
    }
  }

  /**
   * Takes the update values and sets them as the default values
   * then clears the update object.
   * @returns {void}
   */
  applyUpdate() {
    if (this._update && 'value' in this._update) {
      this._value = this._update.value;
    }
    if (this._update && 'completionTimestamp' in this._update) {
      this._completionTimestamp = this._update.completionTimestamp;
    }
    this._update = undefined;
  }

  /**
   * Replaces a File with the CompanyFile that is created when the createPresignedS3Url mutation is called
   *
   * @param {File} localFileId - User uploaded file
   * @param {CompanyFile} companyFile - CompanyFile returned from the API
   *
   * @returns {void}
   */
  replaceFileWithCompanyFile(localFileId, companyFile) {
    if (this.type !== _types.ItemType.PHOTO) {
      throw new Error('ItemResult must be of type PHOTO to replace a file with a company file');
    }
    if (this.value === null) {
      return;
    }
    const values = this.value;
    const index = values.findIndex(value => value.id === localFileId);
    if (index === -1) {
      throw new Error('File not found in ItemResult value');
    }
    values[index] = companyFile;
    this._update = {
      value: values
    };
  }
  removeLocalFiles() {
    if (this.type !== _types.ItemType.PHOTO) {
      throw new Error('ItemResult must be of type PHOTO to remove local files');
    }
    if (this.value === null) {
      return;
    }
    const values = this.value;
    const companyFiles = values.filter(file => !file.id.toLowerCase().startsWith('new'));
    this._update = {
      value: companyFiles
    };
  }
}
exports.ItemResultWrapper = ItemResultWrapper;