import type { JoltRecord, Person } from '../../types';
export declare class JoltRecordWrapper<UpdateObject extends Record<string, any>, Versioned extends boolean = false> {
    protected _update?: UpdateObject;
    readonly id: string;
    readonly createTimestamp: number;
    readonly lastUpdatedByPerson?: Person;
    readonly updateTimestamp: number;
    protected _isActive: boolean;
    constructor(joltRecord: JoltRecord<Versioned> & {
        update?: UpdateObject;
    });
    get isActive(): boolean;
    /**
     * @return {boolean} If the record has unsaved updates.
     */
    get isDirty(): boolean;
    /**
     * @return {boolean} If the record hasn't been saved to the DB yet.
     */
    get isNew(): boolean;
    activate(): void;
    deactivate(): void;
    getUpdateOrDefault<Key extends keyof UpdateObject>(key: Key): (UpdateObject[Key] & {}) | null;
    reset(): void;
    update(update: UpdateObject): void;
}
