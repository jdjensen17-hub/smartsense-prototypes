import { JoltRecord } from '../../types';
import { JoltRecordWrapper } from '../index';
export declare class VersionedJoltRecordWrapper<UpdateObject extends Record<string, any>> extends JoltRecordWrapper<UpdateObject, true> {
    readonly parentId: string;
    constructor(joltRecord: JoltRecord<true> & {
        update?: UpdateObject;
    });
    /**
     * @return {boolean} If the record hasn't been saved to the DB yet.
     */
    get isNew(): boolean;
}
