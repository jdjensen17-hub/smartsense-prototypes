import type { ContentGroup, Location, Item } from './';
export type ItemFlag = {
    id: string;
    parentId: string;
    color: string;
    contentGroup?: ContentGroup;
    isActive: boolean;
    itemTemplates?: Item[];
    location?: Location;
    name: string;
    symbol: string;
};
