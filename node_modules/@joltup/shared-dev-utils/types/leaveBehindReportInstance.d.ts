import type { LeaveBehindReportTemplate } from './leaveBehindReportTemplate';
import type { ListInstance } from './listInstance';
import type { Settings } from './settings';
export type LeaveBehindReportInstance = {
    id: string;
    company: {
        settings?: Settings;
    };
    leaveBehindReportTemplate: LeaveBehindReportTemplate;
    listInstance: ListInstance;
    status: LeaveBehindReportInstanceStatus;
};
export declare enum LeaveBehindReportInstanceStatus {
    PENDING = "PENDING",
    GENERATING = "GENERATING",
    READY = "READY",
    FAILED = "FAILED"
}
