/**
 * Record types supported across Lists templates/instances.
 *
 * Defined locally so `@joltup/shared-dev-utils` can be published without depending on
 * unpublished/private packages that previously defined the same concept.
 */
export type SupportedRecordTypes = 'asset' | 'auditList' | 'list' | 'workOrder';
