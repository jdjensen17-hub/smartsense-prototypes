import type { FileType } from './fileType';
import type { JoltRecord } from './joltRecord';
export type CompanyFile = {
    fileExtension?: string;
    fileURI?: string;
    height?: number;
    html?: string;
    isUploaded: boolean;
    name?: string;
    status?: CompanyFileStatus;
    thumbnail?: CompanyFile;
    type?: FileType;
    width?: number;
} & JoltRecord;
export declare enum CompanyFileStatus {
    UPLOADING = "UPLOADING",
    UN_TRANSCODED = "UN_TRANSCODED",
    TRANSCODING = "TRANSCODING",
    READY = "READY",
    TRANSCODE_ERROR = "TRANSCODE_ERROR"
}
export type ExtendedLocalFile = File & {
    id: string;
    fileURI: string;
};
