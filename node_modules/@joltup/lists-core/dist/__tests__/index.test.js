import * as maskers from '../maskers';
import * as validators from '../validators';
import { Maskers, Validators } from '../';

describe('exports', () => {
  it('exports maskers', () => {
    expect(Maskers).toEqual(maskers);
  });

  it('exports validators', () => {
    expect(Validators).toEqual(validators);
  });
});
