lists-core_test:
  stage: test
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes:
        - lists-core/**/*
  before_script: !reference [.npm_setup, before_script]
  script:
    - npm run lint -w lists-core
    - npm run test -w lists-core
