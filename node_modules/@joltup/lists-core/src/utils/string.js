function isHex(string) {
  return /^[\da-fA-F]{32}$/.test(string);
}

export function isGlobalId(string) {
  const base64regex =
    /^([A-Za-z0-9+/]{4})*([A-Za-z0-9+/]{3}=|[A-Za-z0-9+/]{2}==)?$/;

  return base64regex.test(string) && isHex(string);
}
