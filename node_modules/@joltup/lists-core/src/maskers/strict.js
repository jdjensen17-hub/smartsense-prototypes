import { isNil } from 'lodash';

function StrictMask(value, pattern) {
  if (isNil(value)) {
    return null;
  }

  const matches = value.toString().match(pattern);

  return matches.find((match) => match !== '') || '';
}

export default StrictMask;
