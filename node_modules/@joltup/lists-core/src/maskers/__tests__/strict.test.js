import StrictMask from '../strict';

describe('strict mask', () => {
  test('should default', () => {
    expect(StrictMask()).toEqual(null);
  });

  test('should return empty string when no match', () => {
    expect(StrictMask('Rubber Ducky')).toEqual('');
  });
});
