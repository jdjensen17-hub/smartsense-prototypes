"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _react = require("react");
/**
 * Setup hook for opening and closing something.
 *
 * @param {boolean} original - The original state of open or closed.
 * @param {boolean} shouldReset - If the hook should track changes of the original value and update the internal state
 *
 * @returns {array} hook - Array of value and setters.
 */
function useIsOpen() {
  let original = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
  let shouldReset = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
  const [initialValue] = (0, _react.useState)(original);
  const [isOpen, setIsOpen] = (0, _react.useState)(original);
  const [isReset, setIsReset] = (0, _react.useState)(shouldReset);

  // this allows the user of the hook to toggle the state through a prop change
  (0, _react.useEffect)(() => {
    if (isReset && initialValue !== original) {
      setIsOpen(original);
      setIsReset(false);
    }
  }, [initialValue, isReset, original]);
  const setClosed = (0, _react.useCallback)(() => setIsOpen(false), []);
  const setOpen = (0, _react.useCallback)(() => setIsOpen(true), []);
  const toggle = (0, _react.useCallback)(() => setIsOpen(!isOpen), [isOpen]);
  return [isOpen, {
    setClosed,
    setOpen,
    toggle
  }];
}
var _default = exports.default = useIsOpen;