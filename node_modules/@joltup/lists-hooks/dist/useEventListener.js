"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _react = require("react");
/**
 * Event Listener Hook
 * @param {object}.{eventName} eventName - Triggering event (e.g. 'mouseover', 'keydown', etc)
 * @param {object}.{function} handler - Function to handle the event
 * @param {object}.{object} element - The element to attach the listener to
 */
function useEventListener(_ref) {
  let {
    eventName,
    handler,
    element = window
  } = _ref;
  const usingHandler = (0, _react.useRef)();
  (0, _react.useEffect)(() => {
    usingHandler.current = handler;
  }, [handler]);
  (0, _react.useEffect)(() => {
    const eventListener = event => usingHandler.current(event);
    element.addEventListener(eventName, eventListener);
    return () => {
      element.removeEventListener(eventName, eventListener);
    };
  }, [eventName, element]);
}
var _default = exports.default = useEventListener;