import { PermissionProps } from '@joltup/lists-core';

declare module '@joltup/lists-hooks' {
  export function useIsOpen(
    initialState: boolean
  ): [boolean, { setClosed: () => void; setOpen: () => void }];

  export function usePermissions(overrideCompanyId?: string): PermissionProps | undefined;
}
