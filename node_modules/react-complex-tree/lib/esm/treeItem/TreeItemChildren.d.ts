import React from 'react';
import { TreeItemIndex } from '../types';
export declare const TreeItemChildren: (props: {
    children: TreeItemIndex[];
    depth: number;
    parentId: TreeItemIndex;
}) => React.ReactElement;
