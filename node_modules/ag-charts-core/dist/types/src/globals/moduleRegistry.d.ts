import { type ChartModuleDefinition, type ModuleDefinition, type SeriesModuleDefinition } from '../interfaces/moduleDefinition';
export declare class ModuleRegistry {
    private static readonly registeredModules;
    static [Symbol.iterator](): IterableIterator<ModuleDefinition>;
    static register(definition: ModuleDefinition): void;
    static registerMany(definitions: ModuleDefinition[]): void;
    static reset(): void;
    static detectChartDefinition(options: object): ChartModuleDefinition;
    static detectSeriesDefinition(options: object): SeriesModuleDefinition;
    private static detectDefinition;
}
