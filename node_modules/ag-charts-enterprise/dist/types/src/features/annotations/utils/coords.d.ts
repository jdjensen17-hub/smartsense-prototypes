import { _ModuleSupport } from 'ag-charts-community';
import type { AnnotationContext, Point } from '../annotationTypes';
export declare function snapPoint(offset: _ModuleSupport.Vec2, context: AnnotationContext, snapping?: boolean, origin?: Point, angleStep?: number): {
    x: any;
    y: any;
};
export declare function snapToAngle({ x, y }: _ModuleSupport.Vec2, center: _ModuleSupport.Vec2, step: number, direction?: number): {
    x: number;
    y: number;
};
export declare function getDragStartState<PointName extends string>(points: Record<PointName, Point>, context: AnnotationContext): Record<PointName, _ModuleSupport.Vec2>;
export declare function translate<VectorName extends string>(vectors: Record<VectorName, _ModuleSupport.Vec2>, translation: _ModuleSupport.Vec2, context: AnnotationContext): {
    vectors: Partial<Record<VectorName, _ModuleSupport.Vec2>>;
    translateX: boolean;
    translateY: boolean;
};
